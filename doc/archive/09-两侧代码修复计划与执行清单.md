# 两侧代码修复计划与执行清单

> 依据：[`08-信号侧与执行侧量化交易代码评审报告`](08-信号侧与执行侧量化交易代码评审报告.md)（12 critical + 24 high + 63 medium）。
> 原则：按依赖顺序分批、每批可独立验收、全程不破坏既有绿测、所有触碰代码补中文注释、不含工时。
> 测试环境：执行侧 `astock-quant-ai/.venv/bin/python -m pytest`（qmt_strategy 下，fake 适配器，macOS 可跑全量）；信号侧 `backend/.venv/bin/python -m pytest`。
> 实测口径：标 **[实测]** 的子项依赖目标 Windows/QMT 机，本地只落代码结构 + 安全默认 + 单测，真实数值在目标机用 `vars()/dir()` 固化。

---

## 批次总览与依赖

```
批次1 闭环可用+防重复(P0-A/C) ─┐
批次2 风控护栏生效(P0-B)        ├─► 批次5 P1 高危
批次3 日历+供数口径(P0-E/F) ────┘            │
批次4 go/no-go+回测可信(P0-D) ───────────────┴─► 批次6 P2 中危 + 优化项
```
- 批次 1 是一切前提：闭环不能交易，其它修复无法端到端验收。
- 批次 2 依赖批次 1（风控要接到真实买/卖路径上）。
- 批次 3、4 可与 1/2 并行推进，但上线门槛要求四批全过。

---

## 批次 1 · 让闭环真正能交易且不重复下单（P0-A + P0-C）

| 任务 | 涉及文件 | 修复要点 | 验收标准 |
|---|---|---|---|
| 1.1 strategy_family EN→路由键规范化 | `entry/entry_router.py`、（或 `watchlist/remote_watchlist.py`） | 建 `DABAN→打板/BANLU→半路/DIXI→低吸` 规范化映射，`_select_action` 对英文枚举可命中；保留中文兼容 | 新增用例：DABAN/BANLU/DIXI + 各 setup 至少一条命中非 SKIP 的 action |
| 1.2 买入成交回写持仓状态机 | `data_writer/callbacks.py`、`app/main.py`、`position/position_manager.py` | `ExecCallback` 注入 `PositionManager` 句柄；`on_stock_trade` 对 BUY 成交调 `mark_position_on_fill(today=成交东八区日)` | 集成测试：买入成交→次日 `sellable_units` 非空 |
| 1.3 卖出走唯一下单点 | `order/order_executor.py`、`app/main.py` | `OrderExecutor` 新增 `place_sell`（生成 biz 单号、落台账、纳入幂等/TTL）；`_place_sell` 改调它，禁止直连 trader | 卖单有 biz_order_no、落台账；`order_stock` 仅 OrderExecutor 内调用 |
| 1.4 SELLING 在途单元跳过决策 | `app/main.py`、`position/position_manager.py` | `run_sell_pass` 入口 `state==SELLING → continue`；`_place_sell` 发单前校验状态 | 用例：SELLING 单元不再被二次决策、不二次下单、不抛错中断 |
| 1.5 发单前台账同步落盘 | `order/order_executor.py`、`storage/sqlite_ledger.py`、`storage/write_queue.py` | PLANNED 行在 `order_stock` 前同步落盘（同步 future 或等 task_done）；启动用 QMT 当日委托全量对账兜底 | 用例：insert→落盘→发单顺序；崩溃重启注入后不重复下单 |
| 1.6 biz 序号计数器重启重建 | `order/order_executor.py`、`storage/sqlite_ledger.py` | `load_from_db` 后重建 `_seq_counter`（取已存最大 seq+1），或单号含时间戳/UUID 段 | 用例：含终态失败单时重启不产生同号覆盖 |

> 验收（批次级）：端到端集成测试覆盖"买→持→次日卖→回流→对账"，且仅出一张卖单；崩溃重启场景无重复下单。

---

## 批次 2 · 资金风控护栏真正生效（P0-B）

| 任务 | 涉及文件 | 修复要点 | 验收标准 |
|---|---|---|---|
| 2.1 买入接 risk.gate | `app/main.py` | `_router_sink/run_open` 下单前调 `risk.gate`，FREEZE→拒单留痕、SELL_ONLY_HOLD→禁开仓；纳入 `is_open_blocked` | 用例：账户冻结态买入被拒 |
| 2.2 喂入账户/单票阈值 | `app/main.py`、`risk/risk.py` | 接资产快照(`query_stock_asset`)+持仓浮亏，算 drawdown/realized_loss/unit_float_loss 喂 gate（东八区当日基线） | 用例：三类阈值各能触发 FREEZE |
| 2.3 实现下单次数/总敞口/偏离/黑名单 | `order/order_executor.py`、`risk/risk.py`、`config/settings.py` | 唯一下单点统一校验 `max_orders_per_day`/累计在途+持仓市值 vs `max_total_exposure`/限价偏离 vs `price_deviation_guard_pct`/黑名单 | 用例：四闸各能拦单 |
| 2.4 情绪闸门 hcc 标量化 | `app/services/sentiment_gate.py`（信号侧） | `flatten_gate_inputs` 取 `highest_chain_change["change"]` 标量 + dict 防御；`_num(dict)` 显式告警 | 用例：hcc 正/负两侧分类分支可达 |
| 2.5 market_state 取值域两侧统一 | `app/services/limit_up_push_service.py`、`schemas/limit_up_watchlist.py`（信号侧）+ `watchlist/remote_watchlist.py`、`entry/entry_router.py`、`config/settings.py`（执行侧） | 执行侧消费 `sentiment_cycle`(六档) 做 block + `market_state`(三档) 做总闸；或契约加 `open_gate` 枚举。修正 `watchlist_loader.py:30` 错误注释 | 用例：退潮/冰点/谨慎参与日按预期禁/限开仓 |

---

## 批次 3 · 交易日历同源 + 供数口径修正（P0-E + P0-F）

| 任务 | 涉及文件 | 修复要点 | 验收标准 |
|---|---|---|---|
| 3.1 生产换 DbTradeCalendar + fail-closed | `app/run.py`、`common/trade_calendar.py`（执行侧） | 实现读 `a_trade_calendar` 的 `DbTradeCalendar`（或静态快照同步），与信号侧同源；仅有周末近似日历时 fail-fast/只守仓 | 用例：跨节假日 `prev_open/next_open/earliest_sellable_date` 正确 |
| 3.2 board 涨跌幅补 ST/科创/北交/新股 | `common/board_rules.py`、`common/identity.py`、`limit_up_backtest_service.py`（信号侧） | 涨跌幅表加 ST(0.05)/科创·北交(0.20/0.30)/新股；ST 标志作入参；缺标志降级 MISSING；两侧表同源 | 用例：ST/科创票涨停价正确或降级 |
| 3.3 封流比单位错配修正 | `app/services/limit_up_push_service.py`（信号侧） | `circ_mv` 兜底 ×10000 换元；`seal_ratio_pct` 上界断言（>100% 置缺+质量项） | 用例：free_float 缺数股封流比量级正确 |
| 3.4 竞价两因子供数补齐 | `schemas/limit_up_watchlist.py`、`db/models/notification.py`、`limit_up_push_service.py`（信号侧）+ `watchlist/remote_watchlist.py`（执行侧） | 契约/ORM 增 `first_board_vol`/`float_mktcap`(+可选价位)，信号侧填充、执行侧透传 | 用例：量能比/封流比因子不再恒降级 |
| 3.5 tick 五档归一 | `auction/tick_source.py` 或适配层、`auction/auction_factors.py`（执行侧） | `bidVol/bidPrice/askVol/askPrice` 取 best 档 `[0]`；**[实测]** 目标机核对键名 | 用例：bidVol 为数组时封单因子非 0 |

---

## 批次 4 · go/no-go 闸门 + 回测可信度（P0-D）

| 任务 | 涉及文件 | 修复要点 | 验收标准 |
|---|---|---|---|
| 4.1 go/no-go 判据代码化 | `app/services/limit_up_backtest_service.py`、`db/models/market.py`（信号侧） | 新增 `evaluate_go_no_go(summary)`：最小样本/胜率下限/超额显著为正/IC 显著/p10 不破限 → 结构化 verdict 落 run 表；导出/上线强制读取 | 用例：样本不足/不显著 → NO-GO |
| 4.2 显著性检验 | 同上 | 均值/超额/IC 加置信区间 + bootstrap/t；最小样本门槛 | 用例：小样本标 INSUFFICIENT |
| 4.3 含费含滑点默认口径 | 同上 | `include_fees` 默认 True；加滑点/冲击成本项；summary 并列理想/含成本 | 用例：net 与 gross 双口径产出 |
| 4.4 LLM 概率校准 | 同上 | `continuation_prob`/`next_day_premium_prob` vs 实际续板/溢价的 Brier + 分桶可靠性，纳入 go/no-go | 用例：校准指标产出 |

---

## 批次 5 · P1 高危（详见报告第 4 节）

家族/角色先验消费、CHASE_LIMIT_UP 死代码封单校验、竞价强开追限价封顶、order_phase 误判、龙头打分全池基准、辨识度嵌套解析、counted_trade_ids 类型归一、traded_id 空值去重、ingest account_id 校验、trade_date 成交时间戳口径、午休隔离、日历同源（并入 3.1）。

## 批次 6 · P2 中危 + 优化项（详见报告第 5/6/7 节）

按域逐条；优化项（权重/阈值校准、复权口径、缺测保守降级、概率化加权、可观测性、契约 schema_version 等）在前五批稳定后推进，多数需配合回测校准与目标机实测。

---

## 进度台账

> 每完成一个任务在此勾选并附 commit；批次完成后跑全量回归。

- [x] 批次 1（闭环可用 + 防重复）— 全部完成
  - [x] 1.1 strategy_family 英文枚举路由（5c950d6）　[x] 1.2 买入成交回写持仓（0e884c7）
  - [x] 1.4 在途 SELLING 不重复卖（0e884c7）　[x] 1.3=C1 卖出走唯一下单点+台账（4c09a75）
  - [x] 1.5=C3 发单前同步落盘（5ced0ac）　[x] 1.6=C4 biz 序号重启重建（57e41a8）
- [x] 批次 2（风控护栏）— 主体完成
  - [x] 2.4 情绪闸门 hcc 标量化（4cef275）　[x] 2.1 买入接 risk.gate（b535215）
  - [x] 2.2 喂入账户回撤阈值（b535215）　[x] 2.5 market_state 两侧统一（e9fdbaa）
  - [~] 2.3 已做 max_orders_per_day（f66bc49）；余 max_total_exposure / 黑名单（price_deviation 已并入 P1#3 策略层）
- [x] 批次 3（日历 + 供数口径）— 主体完成
  - [x] 3.1=E1 交易日历 fail-closed + StaticTradeCalendar（24a4dbb）
  - [x] 3.2 board 涨跌幅判不出段降级 MISSING（c01facf）　[x] 3.3 封流比单位错配（a4b6468）
  - [x] 3.5=F4 tick 五档归一（ae8778b）
  - [x] 3.4=F3 竞价两因子供数补齐（信号侧 f491958 + 执行侧 b1e018a）：契约/ORM/迁移0054/落表/透传 +
        量能比阈值重标定。first_board_vol 口径=信号日全天量（首板票即首板量）。
- [x] 批次 4（go/no-go）— D1 完成
  - [x] D1 go/no-go 结构化裁决出口（保守占位阈值，80e6816）
  - [ ] D2 显著性(CI/bootstrap)　[ ] D3 默认含费净口径　[ ] D4 LLM概率校准（均为回测增强，需校准数据）
- [x] 批次 5（P1）— 主体完成
  - [x] P1#1 追买消费强度/续板先验（d2ce9c8）　[x] P1#3/P1#4 限价封顶/order_phase（7498a4a）
  - [x] P1#6 辨识度嵌套解析（ce61e7f）　[x] P1#7/P1#8 traded_id 归一/合成键（bfafec3）
  - [x] P1#9 ingest 信封校验（7679056）　[x] P1#11 午休隔离（56a580b）　[x] P2 卖价用盘口现价（b3726de）
  - [ ] P1#1 余：role 角色在仓位/择时分级消费（需设计）　[ ] P1#5 龙头打分用全涨停池基准（信号侧 pipeline 改）
- [ ] 批次 6（P2 + 优化）— 多为阈值/权重校准、可观测性、复权口径等，需配合回测校准与目标机实测

*基线：执行侧全量绿（438 测试）、信号侧相关模块绿。全程不破坏基线。*

**已完成所有 P0（A/B主体/C全/D1/E1/F1/F2/F3/F4）+ 多数高价值 P1（共 ~29 次代码提交，两仓库均测试+推送）。**
**剩余：D2-D4 回测校准增强；P1#5/role 消费；P2/优化项（多需回测标定/目标机实测）。**

> 部署须知（新增）：
> 1. **信号侧 MySQL 需跑迁移 `alembic upgrade head`**（0054 给 limit_up_selected_stock 加
>    float_mktcap/first_board_vol 两列）；不跑则落表会因缺列报错。
> 2. **执行侧需提供交易日清单文件** `QMT_TRADE_CALENDAR_FILE`（从信号侧 a_trade_calendar 的 is_open=1
>    导出，每行一个 YYYY-MM-DD），否则 E1 的 fail-closed 会拒绝启动；仅离线测试可设
>    `QMT_ALLOW_WEEKDAY_CALENDAR=true` 临时放行。

> 待确认的业务口径（不影响已落地修复，但影响后续批次的最终取值）：
> 1. 半路族(BANLU)当前归入打板族（由 chase 策略买条件二次把关）；如需独立半路战法请确认。
> 2. 「谨慎参与」情绪档是否允许开新仓（批次 2.5 market_state 口径统一时需定）。
> 3. go/no-go 各硬阈值（最小样本量、胜率/超额/IC 下限、p10 红线）的具体取值（批次 4.1）。
> 4. 标 [实测] 项需目标 Windows/QMT 机：xtconstant 常量、get_full_tick 五档结构、DbTradeCalendar 接信号侧表。
