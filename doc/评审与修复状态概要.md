# 评审与修复状态概要

> 本文是「2026-06 正式代码评审 + 修复」的**状态概要**（持久记录）：做了什么评审、修了什么、当前在哪。
> - 详细发现见 `08-信号侧与执行侧量化交易代码评审报告`（明细 `08-评审发现明细.json`）；逐项修复计划与 commit 台账见 `09-两侧代码修复计划与执行清单`。
> - 还需处理 / 只能上生产·目标机实测的事 → [`待办与上线验证清单.md`](待办与上线验证清单.md)。
> - 与 README 分工：README 只记**现状口径**（交易逻辑/架构/部署/资金口径），不记评审与修复状态——本文承担。
> - **注**：本文以 `NN`（如 `08`/`09`/`11`–`14`/`16`–`20`）指代的各轮详细评审报告/台账，已移入**工作区级**归档 `/Users/salty/codeProject/ai/doc/已归档-完成不再维护/`（不再看，按文件名查阅）；本文作为持久概要保留。

## 评审范围与结论

针对本项目「真实投入资金」的属性，做了一次正式代码评审（`08`，166 条发现，确认 **12 critical + 24 high + 63 medium**），并据此完成修复（计划与逐项 commit 台账见 `09`）。**修复已全部测试 + 推送两仓。**

## 修复概要（已全部测试 + 推送）

> **口径澄清（2026-06 go-live 审计补记；2026-06-18 据生产 `21efdcb` 现状更新——卖出链接线已由阶段0-C 落地、生产门控默认关，详见下「整改落地·阶段0-C」与「第五轮」）**：以下「持仓状态机/卖出决策」相关修复均指**决策与状态机逻辑本身已写且有单测**（`sell_decider` / `run_sell_pass` / `_place_sell`）。盘中卖出的**实时盘口→OrderBook 派生 + `sell_books_provider`/`prior_provider` 注入**这段中间接线，**阶段0-C（2026-06）已落地**（`run.py:414` 按 `QMT_SELL_PASS_LIVE` 注入 `engine.build_sell_books`、`run.py:315` 注入 `_build_prior_provider`）；但**生产默认门控关**（`QMT_SELL_PASS_LIVE=False` → provider=None → `scheduler.py:142` 跳过 `run_sell_pass`，回退到接线前的「只守仓不主动卖」安全行为）。故本节及前三轮「卖出已落地」表述应理解为「逻辑+接线均就绪、生产门控默认关、待 T1.2 跨帧数值保真 + 真机实测后才开 `QMT_SELL_PASS_LIVE`」，**非「真机已可卖」**。

- **建仓闭环可用**：修 `strategy_family` 英文枚举与执行侧中文路由失配（原全部计划票落 SKIP、永不开仓）；修买入成交从未回写持仓状态机（原 `_units` 恒空、永不卖出、裸奔扛单）；统一 `market_state` 三档/六档口径（谨慎参与=禁开仓）。
- **风控护栏**：买入接入 `risk.gate` + 喂入账户日内回撤阈值（原买入完全绕过风控、账户熔断对开仓零作用）；情绪闸门 `highest_chain_change`（最高板升降）由 dict 误当标量恒为 0 修复；单日下单次数上限落地。
- **下单/幂等/唯一下单点**：卖出改走唯一下单点 `OrderExecutor.place_sell` 并落卖单台账（原直连 trader 绕过台账/无单号/不可对账）；在途 SELLING 单元不再重复下卖单；发单前同步落盘堵崩溃窗口重复下单；重启重建 biz 序号防同号覆盖；成交去重键跨重启类型归一 + 缺主键合成键。
- **价位/时段口径**：board 判不出段（科创/北交/未知）不再按主板 10% 兜底现算涨停价（降级 MISSING）；卖出限价改用盘口现价（原用成本价炸板卖不出）；竞价限价封顶至涨停价；`order_phase` 按时段判定；午休（11:30–13:00）隔离不发撤/卖单。
- **供数口径**：封流比 `circ_mv` 万元/元单位错配（放大约 1 万倍）修复；竞价虚拟封单 tick 五档归一（原封板场景封单恒 0）；辨识度子分按四维强弱均值分档（原「强恒胜」）；**F3 竞价两因子供数补齐**——契约/ORM/迁移补 `float_mktcap`/`first_board_vol`，执行侧透传 + 量能比阈值重标定。
- **回测/回流**：回测新增 `go/no-go` 结构化裁决出口（原只产指标、无判定；阈值保守占位待校准）；回流 ingest 校验 `record.data` 的 `account_id/trade_date` 与请求信封一致（防串账户/串日）。
- **交易日历 fail-closed**：执行侧不再硬编码「仅排周末」的近似日历（节假日会污染 T+1/名单键/对账），改为必须提供真实交易日清单、否则启动期拒绝（部署动作见 [`待办 §B`](待办与上线验证清单.md)）。

## 仍在路上（去向）

- **上线部署动作**（信号侧迁移 `alembic upgrade head`（2026-06-18 生产已在 0057）、执行侧交易日清单文件、资金/风控开关）→ [`待办 §B 上线部署步骤`](待办与上线验证清单.md)。
- **目标机实测项**（`get_full_tick` 五档结构 / `xtconstant` 常量 / 回报字段等占位固化）→ [`待办 §A`](待办与上线验证清单.md)。
- **非阻塞优化项**（回测显著性检验、默认含费口径、LLM 概率校准、龙头全涨停池基准、role 分级消费等）→ [`待办 §D`](待办与上线验证清单.md) + `09` 剩余项。

> 处理收尾后，08/09 详细处理文档按仓库 `CLAUDE.md` 文档规则移入 `已归档-完成不再维护/`；本概要作为**持久状态记录**保留、随后续评审/修复轮次续写。

---

## 第二轮独立深度评审与修复（doc/11 → doc/12）

针对「真实投入资金」属性，做了**第二轮独立复评**（`11`，不依据旧评审、逐条对抗验证，确认 **92 条：6 P0 + 25 P1 + 39 P2 + 22 P3**），并按系统性根因分**五阶段（A–E）**全部修复，逐项分析/规划/开发/review/测试/提交台账见 `12`。

### 修复概要（五阶段，每阶段经独立审计 agent 对抗复审）
- **阶段A 资金安全与状态可恢复**：写队列 fail-closed（`flush_confirm` 区分已 drain 与已持久 + on_failure 停开新仓）；持仓状态机/台账/TTL/下单计数器崩溃重启可从 QMT 权威 + 本机台账重建（原纯内存、重启即裸奔扛单）；卖出状态机死锁复位（同步失败/拒单/全撤零成交/挂不到价超时四类 revert + 卖单 TTL）；再买入命中 SOLD 开新单元；回调改「内存权威先于 qmt 镜像」；持仓单元加锁防迭代崩溃。
- **阶段B 风控护栏与价位口径真正生效**：行情断流/下单断线/账户回撤/单票止损/限价偏离五道闸从死代码改为真生效（fail-closed 不再 fail-open）；ST 主板涨停 5%、signal_close≤0→MISSING、透传 `name` 识别 ST；对账检出真异常→持久标记阻断次日开仓（SELL_ONLY 至人工清除）+ 触发补采。
- **阶段C 回测裁决与龙头打分可信**：go/no-go 改净收益口径（强制含费）+ 统计显著性，卖出侧防涨停封板顺延、未平仓样本保守 mark 计入；龙头强度分位/分布/最高板基准改由**全涨停池**构建（复审修复了「全池接线断裂、生产路径不生效」的 P0）。
- **阶段D 供数与建仓闭环**：最强龙头（秒封一字/竞价强开）在默认配置下有可执行成交路径（定盘段转 OPENING + 解锁幂等 + 开盘前 TTL 顺延）；半路首板不误路由竞价强开；`first_board_vol` float→int 落表（原恒 NULL、量能比因子结构性降级）；行情取数前先订阅。
- **阶段E 复盘/回流/并发/鉴权**：回流时间 tz-aware→UTC naive（不再错 8h）；当日盈亏差分仅上一交易日成立（不把多日当单日）；回流接口加来源 IP 白名单；COALESCE 防空覆盖扩到所有可空列；写连接 busy_timeout；竞价轮询单轮异常不放弃整段。

### 状态
- **全部已 commit**（执行侧 `astock-quant-ai` main、信号侧 `stock-ah-premium-ai` feature 分支，每阶段一提交，commit message 带 model 后缀）；测试基线执行侧 449→**506 passed**、信号侧评审相关 68→**144+ passed**，全程无回归。
- **少数 P3 经评估为「非纯 bug / 需更大基建」已文档化决策**（如 #66 跨午夜调度、#56/#57 复盘按真实交易日口径、#76/#77 vestigial 配置、#42 龙回头战法分类、#68 转次优），含口径与待办指向，见 `12` §7.2 与决策表 D1–D11。
- 上线硬门槛仍为：阶段A→B→C 通过 + QMT 竞价能力实测 → 小仓灰度（见 `11` §四 与 [`待办`](待办与上线验证清单.md)）。

---

## 信号侧打板报告/watchlist 优化建议落地（doc/10 §11）

针对信号侧 LLM 打板报告与 watchlist 生成链路的优化建议（`10`，27 条对抗式核验建议），先做**落地取舍**（doc/10 §11：基于真实代码复核区分可做/有条件做/暂缓/不做，并发现报告原列 P0「分位池修正」已被第二轮阶段 C/D/E 实现、无需再做），再把「✅ 建议做」全部 + 「🟡 有条件做中无外部前置」子项落地实现。

### 已实现概要（信号侧，详见 `10` §11.8 与信号侧 `resources/doc/limit-up-doc10-landing-implementation-plan.md`）
- **逻辑修复**：§4.5 PARSE_FALLBACK 缓存补 `failed=True`（防极低先验固化污染 watchlist）；§6.2 角色关键词补「高辨识度」+ 否定前缀守卫 + contract-drift 单测（修高连板辨识度龙头被静默压成中性）。
- **打分契约骨架**：§6.1 概率档按 tier 键控（首板下调一档量级）+ `prob_calibration_version` + 列注释澄清离散档值非连续概率；§6.3① `data_confidence` 只读写入 `strength_dim_json`（无迁移）。
- **流程**：§4.2 空仓闸门 `resolve_gate` 前移注入 `market_context`（含 SELECTION 精简闸门视图），落表段确定性 re-resolve、口径与 prompt 逐字节一致，消除「报告正文↔watchlist 行级动作」冲突。
- **提示词**：§5.6 清除诱导越界（仓位/低吸/止损→倾向/失效条件）+ 禁空泛套话；§5.1 阶段化字段白名单（FOCUS 不含资金字段）；§5.3① chain/high 资金措辞+缺测中性；§5.4 一字板提示；§5.2 经验阈值搬运；§5.5 周期降级复用权威 gate。
- **配置/契约**：§4.7 三层精选上限砍半（35→约 18，纯配置）；§3.2 watchlist 龙虎榜三只读列 + Alembic 0056 + `WATCHLIST_SCHEMA_VERSION`→1.1.0（抓取失败→None 未知、缺测不置 0）；§3.6 数据接入红线 + 单一资金口径硬约束写入信号侧 `resources/doc/limit-up-data-access-redlines.md`。

### 状态
- 经**两轮多智能体对抗式代码评审 + 修复**（第一轮发现并修 1 P1 真实缺测 bug「top_list 抓取失败被臆断为不在龙虎榜」、1 P1 提示词字段不一致等 6 项；第二轮发现并修 1 P2「闸门视图重复序列化」）。
- 信号侧测试 **466 passed**（无回归）、改动文件**零新增 ruff 违规**；已 commit 至信号侧 `stock-ah-premium-ai` feature 分支、doc 同步至 `astock-quant-ai` main。
- **暂未做**（外部硬前置/待闭环样本）：§3.1 moneyflow 资金主线（需积分档核验+覆盖率+回测同批迁移）及其依赖链、游资/板块/大盘资金、§6.4 权重回归等依赖实盘 `qmt_*` 回流样本的校准项——承接见 `10` §11.2/§11.3/§11.8。

---

## 第三轮独立深度评审与修复（doc/13 → doc/14）

针对「真实投入资金」属性做了**第三轮独立复评**（`13`，明细 `13-评审发现明细.json`，确认 **72 条：3 P0 + 14 P1 + 55 P2**），按系统性根因分**门槛 A–E + 模块迭代**共 **13 个批次全部修复**，逐项设计/开发/测试/对抗评审/修复/复测/提交台账见 `14`。**每个批次的对抗式 review 都真抓到至少一个「看似已修实则有 bug」的问题（含把 P0/P1 修出回归），回炉重修 + 补测后才交付。**

### 修复概要（按门槛，每批经独立审计 agent 对抗复审）
- **门槛 A 资金安全（P0+P1，批次 1–5）**：连接 `session_id` 69 位溢出 xtquant int → 钳到 int32、心跳冻结通道无解冻路径 → 接 reconnect、重连每次重建 trader 泄漏 → 预取 session 单次连接；下单 `CANCELLING` 首次进态未续 TTL deadline 致兜底失效 → 续 deadline；持仓字段级写全纳 RLock + `get_unit` 深拷贝 + 原子卖出/在途冻结/盘前卡死对账；回流合成 traded_id 防成交折叠、`TradeSide.UNKNOWN` 不臆造 BUY（reconcile 作通配 + 资产对账降级）、对账 NULL 防御 + 分步隔离；`resolve_code` 锚点化后缀 + 矛盾脏串返 None、`is_st` 显式布尔不把缺名当 ST 漏买。
- **门槛 B watchlist/报告正确（批次 9–10）**：龙头打分把筹码摘要 `cyq_summary` 按 ts_code 注入位置筹码维（原 `chip_safety` 恒中性 0.5）、多题材串按单题材归并防板块龙头滥发；选股链路 READY 早退补落选股（从 `context_json` 还原含 pipeline 的 context）、`winner_rate` 取嵌套键、LLM 三态 + 兜底候选 BLOCKED 降级、建议成功后回填 `advice_degraded`。
- **门槛 C 回测 go/no-go（批次 12）**：统计显著性 t 检验改用**独立交易日数 + 组合日收益样本标准差**（原用全可成交样本数 + 总体标准差，把横截面/时序相关样本当独立观测使 t 随 √n 系统性虚高、放行偶然策略）；`v_exec.0` 乐观分档（秒封按开盘价记可成交）入口硬校验禁产 GO。
- **门槛 D 复盘/回流/鉴权（批次 8）**：QMT 闭环 join 带 (account_id,trade_date,order_id) 三元组、`_signal_map` 按生效 prompt_version 消歧、回流信封必填强一致防串账户/串日、净值剔出入金用存量 `daily_return`、回流写 token 不回落读 token、watchlist 导出补来源 IP 白名单。
- **门槛 E/F 韧性·观测·模块迭代（批次 6/7/11/13）**：存储写队列 fail-closed + 台账持久化健壮性（成交明细表防崩溃二次累计、`load_from_db` 窗口装载 + 活跃单索引、跨日 `order_id` 两级反查防串单、`set_flag`/`save_watchlist` 等锁重试）；竞价/建仓强度权重（仅 top-N 进权重护龙头名额防优先级反转、缺强度保守份额、BUY `plan_volume` 缺失 fail-closed 拒单、低吸取较低价不追价、量能比量纲不对等降级留痕、竞价轮询两遍 push 隔离）；报告投递并发护栏（CAS 占位防双发）+ 公开分享防护（永久链接默认禁用、view_count 原子自增、域名白名单）+ MANUAL 幂等/FAILED 有限重试；选股供数 `pct_chg` 关联补列、universe 退市 point-in-time 剔除、指数成分抓取失败兜底、桶配额保底。

### 状态
- **全部 13 批已 commit**（执行侧 `astock-quant-ai` main、信号侧 `stock-ah-premium-ai` feature 分支，每批一提交、commit message 带 model 后缀）；测试基线执行侧 **593 passed**、信号侧 **493 passed**，全程无回归。
- **3 个 P0 + 14 个 P1 全清零**；55 个 P2 全部落地（含批次 6 一度延后的 order-01/order-06/storage-05 三个低优先持久化加固已收尾，对抗评审确认无漏判活跃单/无迁移擦写/无跨日串单三类致命风险）。
  - **澄清（go-live 审计补记）**：此处「全清零」指三轮评审**显式登记的 P0/P1/P2 条目**已修，**不等于整体可上线**——盘中卖出链接线缺口被历轮当「待实测 TODO」延后归桶、**从未实现**，不在「13 批全清零」统计口径内（详见末段「第四轮」与 [`待办 §0`](待办与上线验证清单.md)）。
- 部分**经验占位常量待回测/实测标定**（竞价量能比 `_VOL_RATIO_MIN`、缺强度份额系数 `_MISSING_STRENGTH_FACTOR`、go/no-go 阈值等）与**目标机回报字段固化**承接见 [`待办`](待办与上线验证清单.md)；上线硬门槛仍为门槛 A→B→C 通过 + QMT 竞价能力实测 → 小仓灰度。

---

## 第四轮：go-live 可行性审计（2026-06）

针对「真实投入资金、即将上线」属性，对信号侧 `stock-ah-premium-ai` + 执行侧 `qmt_strategy` 做了一次**上线可行性专项审计**（多智能体并行追踪运行期路径 + 对抗式核验，45 条确认发现）。结论：**唯一硬阻塞是 1 条代码缺口（盘中卖出链未接线），其余为 minor 健壮性/可观测性/死代码建议**；并须把「代码项」与「真机才能验证的实测项」明确分开。

### 1 条代码级硬阻塞：执行侧盘中卖出链未接线（必须离线补齐）
- **现象**：`run.py:332-334 sell_books_provider=None`（无 env 开关）、`scheduler.py:142` provider 为 None 即 `return` → `run_sell_pass` 永不执行；全仓**非测试代码零 `OrderBook()` 构造点**。⇒ 买入后**结构上无法主动卖出**：止损 / 炸板 / 破位 / 连板了结 / T+1 常规卖出**全不触发**，等同裸奔扛单。
- **性质**：**不是**「真机绑定、只能上目标机验证」的实测项，而是一段**可离线写 + fake 单测**的代码缺口。卖出**决策逻辑 `sell_decider` 与下游状态机 `run_sell_pass`/`_place_sell` 已写且有单测**，缺的只是中间「实时盘口 → `{ts_code: OrderBook}` 派生 + provider 函数 + 注入」；买入侧 `auction/auction_factors.compute_auction_factors` 即同构样板。
- **历史与状态不实纠偏**：前三轮均点过此缺口（08 报告:238 记 **P2**、codex **P1-2.9** 且建议「未装配前 README『持仓/卖出已落地』口径应改」、第三轮 13 报告 `EXEC-risk-04`/`EXEC-position-08`）。**但 14 台账把 `EXEC-risk-04`/`EXEC-sched-05` 标「✅完成」对卖出链不成立**——其「完成」仅指「provider 为 None 时保守不卖 + 留好 `decide_auction` 调度入口」，**并未接线 provider**，`run_sell_pass` 生产仍不可达，593 绿是 None-provider 路径的单测绿。codex 关于改 README 口径的建议**至今未执行**（本轮已修：README §6 卖出口径降级）。故此缺口属**延后未做**、非**已修复**，不计入「全清零」口径。
- **承接**：作为上线前**最高优先级代码任务**写入 [`待办 §0`](待办与上线验证清单.md)。

### 1 条非阻塞但会静默断流的配置陷阱：两侧 ingest token 回落不对称
- 信号侧 `resolve_qmt_ingest_internal_token`（`backend/app/core/config.py:468`）**绝不回落**到 watchlist 只读 token（SIG-QMT-06 安全口径，正确）；执行侧 `resolve_ingest_token`（`config/settings.py:309`）未配专用 token 时**回落** `QMT_SIGNAL_INTERNAL_TOKEN`。⇒ 信号侧若未**单独**配 `QMT_INGEST_INTERNAL_TOKEN(_FILE)`，`POST /internal/qmt/ingest` 恒 503、**回流静默断流**。`routes_qmt_ingest.py:8` 注释「默认复用 watchlist token」与实际相悖（过时，本轮一并清理）。承接见 [`待办 §F`](待办与上线验证清单.md)。

### 代码项 vs 真机实测项（勿混）
- **代码项（可离线，须上线前清）**：① 卖出链接线（硬阻塞，见 [`待办 §0`](待办与上线验证清单.md)）；② 一批 minor 健壮性/死代码/可观测性（`assert_safe_to_trade` 空 no-op、`FROZEN` 态无赋值、SYNC 部分失败不告警、`account_id`/`mini_path` 无 fail-closed、`makedirs` 缺守卫、`QMT_AUCTION_WINDOW` 惰性配置等），择机处理、不阻塞上线（见 [`待办 §D`](待办与上线验证清单.md)）。
- **真机实测项（只能上目标机验证，不属代码缺陷）**：`get_full_tick` 五档结构 / `xtconstant` 常量 / 回报字段固化 / 竞价能力实测等，见 [`待办 §A`](待办与上线验证清单.md)——**勿与卖出链接线混为一谈**（后者能离线写完）。
- **已核对一致**：执行侧本机 425 / 目标机 593 单测全绿；两侧 HTTP 契约（字段名 / `T`,`T+1` 日期 / `X-Internal-Token` / 含 `trade_date` 幂等键 / UTC↔东八区）一致。

> **更新后的上线硬门槛**：§0 卖出链接线补齐（代码·离线）+ 信号侧单独配 ingest 写 token（部署）+ 门槛 A→B→C 通过 + QMT 竞价能力实测 → 小仓灰度。**卖出链未接线前不得接实盘**（买入即裸奔无止损）。

---

## 国金 QMT 对接专项核对（2026-06：xtquant API 正确性 + 代码完成度复核）

针对用户即将接入**国金证券 miniQMT** 实盘，做了一次专项核对（多智能体并行：5 路代码调查 + 2 路联网核对迅投 xtquant 源码/国金资料 + 4 路对抗式复核）。**结论：代码层结论与前四轮一致，无新增硬阻塞；xtquant 调用代码基本正确，多数「占位」实为标准正确值。** 详细发现与裁决见 [`待办 §0/§A/§D`](待办与上线验证清单.md) 与分阶段开发计划 `16-卖出链接线与上线前代码缺口开发计划.md`。

### A. xtquant / 国金 API 调用正确性（联网核对迅投源码确认）
- **关键好消息**：代码中标「占位/待实测」的值经核对迅投 `xtconstant.py` / dict.thinktrader.net **实为标准正确值**——`STOCK_BUY=23`/`STOCK_SELL=24`/`FIX_PRICE=11`、委托状态 `48–57` 映射（54 已撤/56 已成/57 废单三终态无偏移）、`order_stock` 8 参顺序、7 个回调方法名与签名、`StockAccount(account_id)`（纯现货）、`connect/subscribe==0` 判就绪、`get_full_tick` 返回结构与 `lastPrice/lastClose/volume/bidVol/bidPrice` 键名、五档为 list 取 `[0]`、`subscribe_quote(code,'tick')`。
- **代码主动规避的两个常见坑（已用对）**：① 持仓证券代码用 `stock_code`（非 `m_strInstrumentID`，后者会取空）；② 同步失败判 `order_id is None or <0`（正确覆盖官方失败码 -1，优于很多封装误写 `is None`）；③ `session_id` 收口正 int32（规避 `int(time.time()*1000)` 量级的 C 层溢出）。
- **国金 = 迅投 QMT（ThinkTrader/iQuant）定制版**，xtquant 完全通用；`userdata_mini` 路径形如 `D:\国金证券QMT交易端\userdata_mini`，`account_id`=资金账号，Python 须 64 位 3.6–3.12 且先登录 miniQMT。**须向国金客户经理确认**（影响能否上线）：个人是否开放程序化下单、程序化交易权限协议/报备、L2 是否收费及 miniQMT 可用、流控阈值（见 [`待办 §A`](待办与上线验证清单.md) 末尾）。
- **性质澄清**：§A 实测是「上真机 `vars()` 核对去掉占位字样」的最终确认，**非「现在写错了等着改」**；唯需逐回调/首笔实盘验证回调确被触发（方法名错无 getattr 兜底）。

### B. 代码完成度复核（与前四轮一致 + 新增/细化项）
- **唯一硬阻塞仍是盘中卖出链未接线**（[`待办 §0`](待办与上线验证清单.md)），对抗式复核以更强证据坐实（全仓含 tests 零生产 `OrderBook()` 构造、无 `[project.scripts]` 旁路、`close_batch`/`sweep_ttl` 不发卖单）。本轮**细化**了字段映射规格：4 个单帧字段可离线落地、7 个跨帧状态量须真机 tick 键名实测；并明确 `prior_provider` 同根未接 → 持仓恒 `TECH_EXIT`（续板增强不生效）。
- **前轮 §D 已登记项全部证实属实**：`assert_safe_to_trade` 空 no-op 且无调用方、`PositionState.FROZEN` 无赋值点、`QMT_AUCTION_WINDOW`/`QMT_INTRADAY_SNAPSHOT_MINUTES` 惰性配置、`try_next_best` 死代码、`account_id`/`mini_path` 无 fail-closed、`makedirs` 缺守卫、SYNC 部分失败不告警。
- **本轮新发现 4 项（前轮文档未登记，已补入 [`待办 §D`](待办与上线验证清单.md)）**：`_STATUS_NUM_MAP` 缺 `255`(ORDER_UNKNOWN)、`cancel_order_stock` 两处未包 try/except、`QMT_ACCOUNT_TYPE` 全链路惰性、MySQL 回落 `conn_factory` 为 `NotImplementedError`（默认 HTTP 路径不触发）。
- **文档纠偏**：README §6 卖出行「非真机绑定」措辞过宽 → 已改为「provider 接线/注入 + 4 单帧字段可离线，破位/炸板等 7 跨帧字段须真机 tick 实测」；§A 增「占位值已核对为标准正确值」澄清，避免误读为高风险。（代码注释「QMT 无 on_connected」亦不准确——xtquant 实有 on_connected，代码靠 `connect()` 返回值判就绪是官方推荐做法、设计无误，仅注释措辞待改，列入 `16`。）

### C. 整改落地（阶段0 离线代码，2026-06，分阶段开发计划见 `16`）
按「规划→开发→测试→对抗式 review→修改→复测→同步文档」分三批落地，全量测试 593→**618 passed** 无回归：
- **阶段0-A（fail-closed + 清理，commit 6d1e81e）**：`build_real_engine` 缺 `account_id`/`mini_path` 拒启（移除 UNKNOWN/空串回落）；`assert_safe_to_trade` 实现竞价择时未实测放行即拒启（新增 `QMT_AUCTION_TIMING_VERIFIED`）；删除惰性配置 `auction_window`/`intraday_snapshot_minutes`；MySQL `conn_factory` 配 DSN 即启动期报错；`QMT_ACCOUNT_TYPE`/`FROZEN`/on_connected/subscribe_quote 注释订正。
- **阶段0-B（健壮性，commit ab78ed1）**：`_STATUS_NUM_MAP` 补 `255→REJECTED`；`OrderExecutor._safe_cancel`（撤单异常不打断 sweep）；`LocalStorage.start()` 父目录 makedirs；SYNC ok=False 升级 error + `resync_pending`/`pending_dates` 重同步入口；order_remark 长度注释。
- **阶段0-C（卖出链接线 + prior_provider）**：`sell_book_builder` + `Engine.build_sell_books` + `_build_prior_provider` 注入；**对抗式 review 抓出 B1 误清仓 blocker**（不在今日 watchlist 的真封板隔夜票 is_sealed 误判 False → decider 兜底误清；watchlist 取数失败批量误清）+ H1 止损假象 → **以生产门控 `QMT_SELL_PASS_LIVE`（默认关，开时强制配单票浮亏止损）收口**：阶段0-C 只交付接线代码，生产卖出默认不开，待阶段1 T1.2 跨帧保真 + 隔夜持仓补数 + 真机实测后再开。
- **真机实测项（阶段1，依赖国金 miniQMT）**：§A 占位常量/字段/结构确认（A1–A10）、跨帧盘口字段保真（T1.2，开 `QMT_SELL_PASS_LIVE` 前置）、竞价择时实测（A4）。


---

## 第四轮评审与修复（2026-06：多智能体致命 bug 评审 + 禁买 ST 硬化，详见 `17`）

针对用户「查找致命问题」诉求，做多智能体并行评审（12 子系统并行深读 + 对抗式逐条核验，22 候选→**确认 19 条**），随后按「设计→开发→测试→review→修复→测试→review→修复→测试→同步文档→commit」全部修复，并新增**「绝不买入 ST」硬规则**。全量测试 618→**645 passed** 无回归（新增回归用例 `tests/test_review_fixes_2026_06.py`）。

### 确认并修复的问题（19 条）
- **高**：F02/F16 卖出在途量 `on_road_sell_volume` 泄漏（部成剩余委托终态失败/跨日不清零 → 永久漏卖、止损卖不出）；F08 `is_st` 全程丢失（主板 ST 按 +10% 误算涨停价 + ST 拦截单点押 name）。
- **中**：F18 低吸档缺省回退把「合理高开区间」当低吸档（低吸族买卖方向相反）；F01 资产对账成交净额(无费用) vs 现金变动(含费用) 硬比 + 阈值硬编码（高换手日误报封次日开仓）；F14 日初权益基线缺失时 ceiling 退化全额现金（绕过 `TARGET_POSITION_RATIO`）；F03/F13 单票上限未扣已有持仓/已成单双重扣减；F15 回撤闸缺基线 fail-open；F06 `account_loss_limit` 配了永不喂数；F09 写线程卡死(hang) `is_healthy` 误报健康；F07 写队列未武装 `max_queue`；F21 对账反向勾稽跨日复用 order_id 掩盖手工单；F11 空仓日 PREWARM 反复重连重抓。
- **低（防御）**：F19 负强度权重无防御；F12 prefetch 非对象信封未校验；F05 单票浮亏阈值 gate(金额) vs 活体(比例) 口径冲突（gate 侧死代码）。
- **按既定决策处置**：F04（分时跨帧扳机最小版恒 False，依赖真机 T1.2）保持 `QMT_SELL_PASS_LIVE` 门控 + 文档标注；F10（`flush_confirm` 全局标志，fail-closed 安全）、F17（量权威单元同日加仓自愈）记可接受边界。
- **架构澄清（非 bug）**：9:30 后无盘中买入轮询属有意设计（竞价定夺 + 挂 OPENING 单待开盘成交），README §3 已澄清口径。

### 绝不买入 ST 硬规则（新增）
三层闸叠加：盘前 universe 剔出可交易名单 + 建仓路由 SKIP + 唯一下单点 `place` 最终拒单；统一口径 `is_st_stock`（显式 `is_st=True` 或证券名含 `ST/退`）；`is_st` 经 HTTP→SQLite(`watchlist.is_st` 列+迁移)→`PlanRow`→`EntryDecision` 可靠透传（堵 F08）。

### 两轮 review 发现并修复的「修复未彻底/新引入」缺陷（5 条）
F02 同日回扣死路（`sync_status` 收口 PART_TRADED → 改按回报原始状态判定）、多单在途零成交误清、on_road 回扣非幂等（按 `order_id` 去重）、F19 全非正分退化等权、`QMT_WRITE_QUEUE_STUCK_SECONDS=0` 被 `or` 吞。二轮 review 复核通过。

### 新增/变更环境变量
`QMT_WRITE_QUEUE_MAX`(默认 50000)、`QMT_WRITE_QUEUE_STUCK_SECONDS`(默认 30)、`QMT_RECONCILE_ASSET_ABS_FLOOR`(默认 1000)、`QMT_RECONCILE_ASSET_REL_RATE`(默认 0.003)；`QMT_AUCTION_LOWBUY_PCT_LOW/HIGH` 口径强化（未配则低吸族 fail-closed）；`QMT_ACCOUNT_LOSS_LIMIT` 启动期强告警（未独立接线）。详见 README §8.1。

> **上线硬门槛不变**：盘中卖出链开 `QMT_SELL_PASS_LIVE` 仍须 T1.2 跨帧保真 + 真机实测；本轮修复不改变该门槛，但把「开门后」的卖出在途量泄漏、ST 误买、资金/对账口径等致命/严重缺陷清零。

---

## 第五轮：预上线致命问题独立评审与修复（doc/19 → doc/20）

针对执行侧「真实投入资金、即将上线」属性，做了一次**预上线致命/严重问题独立评审**（`19`，多路并行深读 + 逐条对抗式回源码核验，确认 **3 致命 C-1/C-2/C-3 + 4 严重 H-1~H-4 + 2 中等 M-1/M-2**），并按「分析→设计→开发→测试→review→修复→测试→review→同步文档→提交」全部处理，逐条台账见 `20`。**可离线消除的缺陷全部已修并测；外部真机依赖项（C-1 开卖出门、C-3 跨帧数值保真）按评审要求先跳过并登记。** 全量测试 667→**702 passed** 无回归（净增 35 用例）。

### 修复概要（每条均经独立对抗 reviewer 复核）
- **卖出链逻辑（C-2/C-3）**：C-2 `decide_auction` 补「实时封板(`is_sealed`)+封流比强→HOLD」分支，修正竞价正封涨停的弱先验隔夜票被误判「弱开」清仓（与分时秒板续持同口径）；C-3 `build_order_book` 增 `cross_frame_builder` 接线扩展点 + Engine `set_sell_cross_frame_builder` 注入点（默认 None 保守默认、向后兼容），跨帧字段**数值保真**留待 T1.2 真机。
- **异步回报/重连并发（H-1/H-2）**：H-1 `sync_status` + `reconcile_fills_from_detail` 双加「已 TRADED 不回退」守卫（用真实终态白名单、**不**用含 PART_TRADED 的 `terminal()`），堵乱序回报/启动重建把已全成单降级 → 对已成单反复发撤单 + 对账污染；H-2 `connection_guard` 新增 `_conn_lock`(RLock) 非阻塞互斥串行建连全序 + 替换前 best-effort 停旧 trader，堵多线程并发重连的多 trader/后台线程泄漏 + 句柄漂移。
- **风控/调度 fail-open 与不可恢复单点（H-3/H-4）**：H-3 `assert_safe_to_trade` 增账户回撤闸/限价偏离闸装配期 fail-closed 强校验（缺配拒启）+ `_limit_price_sane` 偏离闸缺盘口现价时 fail-closed 拒发；H-4 PREWARM 连接未就绪不定稿、持续重试到连上（连接恢复后 prewarm 幂等重跑补齐持仓/权益基线、收盘为界不无限）+ RUN_AUCTION 成功跑完才标 fired（异常下轮重试，竞价相位可恢复）。
- **持久化/资金口径（M-1/M-2）**：M-1 `add_fill` 镜像+明细、`purge_before` 双删各合并为单写任务（同事务原子）+ `load_from_db` 明细窗口收敛防孤儿无界增长；M-2 `_held_value` 回传 `held_known` 标志，离线 `_plan_volume` + 强度加权 `_strength_budget_volume` 两消费点在持仓查询失败时 fail-closed 拒单（堵跨日同票超单票上限）。

### 对抗式复核（review）抓到并已修的真问题（2 条 HIGH）
1. `reconcile_fills_from_detail` 缺「已 TRADED 不回退」守卫（H-1 的启动重建对称漏洞）——已补守卫 + 测试，使台账三处状态收口口径全对齐。
2. `_limit_price_sane` 偏离闸在盘口现价缺失时静默放行（H-3 未覆盖的运时 fail-open）——已改 fail-closed + 测试。
> 复核另误报 2 条 high（账户回撤闸运时缺数据 fail-closed **早已实现**于 `_open_blocked_by_risk:585-600`、`>`/`>=` 两闸语义不同各自自洽），已澄清剔除。

### 状态
- **C-2/H-1/H-2/H-3/H-4/M-1/M-2**：✅ 离线全部修复并测试，对抗复核通过。
- **C-3**：✅ 接线扩展点离线已修；⏳ 跨帧字段数值保真待 T1.2 真机（已登记 [`待办 §0/§A`](待办与上线验证清单.md)）。
- **C-1**：现状=`QMT_SELL_PASS_LIVE` 默认关门控；⏳ 开门控待 T1.2 跨帧保真 + 真机实测（外部依赖，不翻默认）。
- **新增部署要求（H-3）**：生产 `run_qmt.bat` 须显式配 `QMT_ACCOUNT_DRAWDOWN_LIMIT` + `QMT_PRICE_DEVIATION_GUARD_PCT`（否则装配期 fail-closed 拒启），已登记待办。

> doc/19 + doc/20 因仍有真机后续项（C-1/C-3 保真）暂留 `doc/` 根、未归档；待真机项收尾后整体归档，本概要持久留存。

---

## 第六轮：全链路独立评审（doc/21，仅评审待修复）

针对全链路「真实投入资金、模拟上线 collect-only」属性，做了一次**全链路代码逻辑/交易严重 bug 独立评审**（[`21`](21-全链路代码评审-交易严重bug独立评审报告.md)，18 个子系统并行精读 → 逐条对抗式回源码核验 → 评审人独立二次确认）：18 条候选，**核验通过 14 条（6 严重 + 8 中等）、证伪/降级 4 条**。**本轮为评审报告、修复尚未开始**（与前五轮"评审+修复全清零"不同）。

### 确认的严重问题（6 条，开门控前硬阻断）
- **B1 部成单无限重撤 + 死量预算超额承诺**（`PART_TRADED` 同属 `terminal()`/`active()` 且无对称守卫，`order_executor.on_ttl_expired` + `local_ledger.sync_status` + `enums`）——开 `KILL_SWITCH` 后暴露。
- **E1 建仓幂等锁先于编排层风控/限价闸**（`_router_sink` 拒单分支不 `release`，单帧瞬时拒单→该票全天该买不买）——开 `KILL_SWITCH` 后暴露。
- **E2 `EntryRouter._decided` 跨日永不重置**（`prewarm` 漏重置，昨日成交标的次日被锁死不建仓，与"跨日复买连板龙头"设计自相矛盾）——进程跨日常驻 + 开 `KILL_SWITCH` 后暴露。
- **C1 交易日历末日越界 fail-open**（`StaticTradeCalendar.next_open` 越界 `raise` 被买入回写吞掉→已成交持仓丢出状态机隔夜裸奔；`rebuild` 路径级联致全天调度瘫痪）——静态日历未及时外延 + 当日有买入成交时暴露。
- **P1 盘中重连重建对 SELLING 单元只校准量不清在途/不复位**（`rebuild_from_broker_positions` 不调 `reconcile_stuck_selling`，整日卡死漏卖止损失效）——开 `QMT_SELL_PASS_LIVE` + 盘中重连后暴露。
- **P2 竞价段正封涨停但封流比不可得→误判"弱开"清仓砸自家封板**（`_is_seal_strong` 押在常缺的 `float_mktcap`，是 doc/19 C-2 想修场景的残留缺口）——开 `QMT_SELL_PASS_LIVE` + `float_mktcap` 缺失时暴露。

### 确认的中等问题（8 条）
- **B2** 负哨兵 order_id 写入反查索引击穿"未知单忽略"防线；**P3** 分时段同源封板误清（同 P2）；**P4** `_is_seal_strong` 仅判 `ratio>0` 非战法强封阈值致弱封票误持拒卖；**E3** `AuctionPoller._history` 跨日不重置内存泄漏+tick_seq 失真；**R1/R2/R3** 对账（台账侧 ERROR 行未排除抢报 / 卖单弱关联仅 ts_code 取首条张冠李戴 / 孤儿成交不报警不补采）；**S1** 信号侧导出契约无 `is_st` 字段致执行侧禁买 ST 退化为单点 name 判定（权威 `is_st_on_date` 已算出但未持久化）。

### 共性根因（一次改受益多条）
`_decided` 释放/重置（E1+E2）、`_is_seal_strong` 封板质量口径（P2+P3+P4）、`PART_TRADED` 双重归属（B1）、对账关联（R1+R2+R3）。证伪 4 条与潜在风险（`try_next_best` 绕过限价偏离闸，因 `next_best` 恒空当前不可达）见 doc/21 附录。

### 修复（doc/22 逐条台账，处理中）
按 doc/21 §3 依赖分组逐条处理（每条走 设计→开发→测试→review→修改→review→同步文档→提交），逐条台账见 [`22-全链路代码评审修复台账`](22-全链路代码评审修复台账.md)。基线 702 passed。
- **B1**（严重，部成单无限重撤 + 死量预算超额承诺）：✅ 已修并测。新增独立终态 `OrderState.PART_CANCELLED`（部成撤单终态收口，退出可撤集合/不重建 TTL/remaining 不占承诺/三处不回退守卫粘滞，仍保 find_active 幂等与建仓名额），对抗式 review 通过，705 passed。
- **E1/E2/E3**（严重/严重/中等，建仓路由幂等锁与竞价采集的当日状态）：✅ 已修并测。E1 在 `_router_sink` 风控/限价拒单分支补 `release`（瞬时拒单解锁重评）+ 复审补 `_orch_block_logged` 去抖防洪泛；E2 新增 `EntryRouter.reset_day()`、E3 新增 `AuctionPoller.reset_history()`，均由 `prewarm` 每日调用。两轮对抗式 review 通过，709 passed。
- **C1**（严重，交易日历末日越界 fail-open）：✅ 已修并测。新增 `_safe_next_open`/`_safe_prev_open` fail-closed 占位（保证已成交持仓不被丢出状态机、rebuild 不崩溃穿透 prewarm）+ rebuild per-item 兜底 + `trading_days_left` 覆盖度预警；review 同源补 `reconcile.backfill_signal_trade_date` 越界兜底。T+1 安全由券商 can_use_volume 权威兜底，714 passed。
- **B2**（中等，负哨兵 order_id 写入反查索引）：✅ 已修并测。同步下单失败不再把负哨兵写入 order_id（保持 None），索引零污染、负值回报永不串到失败单，失败码改入 error_msg。715 passed。
- **第一批完成**（B1/E1/E2/E3/C1/B2，开 `KILL_SWITCH` 前硬阻断 + 配套）：✅ 全部修复并测、各两轮 review，702→715 passed。
- **P1**（严重，重连重建对 SELLING 单元不复位）：✅ 已修并测。`on_reconnect_backfill` 在 rebuild 后补调 `_reconcile_stuck_selling`（复用盘前券商委托终态对账），复位断线期卡死的 SELLING 单元、防整日漏卖止损失效。716 passed。
- **P2/P3/P4**（严重/中等，`_is_seal_strong` 封板质量口径）：✅ 已修并测。新增 `_decide_sealed_board` 三态分流——封流比不可得→保守续持 HOLD（修 P2/P3 砸自家封板，无条件生效）、强封→HOLD、弱封→先验弱 REDUCE 不全清（修 P4，需配 `QMT_SEAL_RATIO_MIN>0` 激活）；`is_sealed=True` 一律在此收口、绝无砸封板路径残留。720 passed。
- **R1/R2/R3**（中等，对账关联缺陷）：✅ 已修并测。R1 入口排除 ERROR 台账行 + 强关联校验 consumed；R2 强关联优先两遍法 + 卖单弱关联多候选歧义告警；R3 补孤儿成交（有成交无委托）检测 + needs_backfill。724 passed。
- **第二批完成**（P1/P2/P3/P4/R1/R2/R3，开 `QMT_SELL_PASS_LIVE` 前硬阻断 + 对账）：✅ 全部修复并测、各 review。
- **S1**（中等，信号侧导出契约无 `is_st`）：✅ 已修并测（信号侧 `feature/limit-up-watchlist-signal` 分支）。补 ORM `is_st` 列 + 迁移 0058 + 持久化数据驱动值 + 契约字段透传；口径诚实——补全显式 is_st 契约层、不改当前 ST 拒买行为（ST 仍信号侧过滤 + 执行侧 name 兜底双重保障）。信号侧 501 passed。部署须跑 `alembic upgrade head`(0057→0058)。

### 第六轮总结（doc/21 → doc/22）
**14 条全部修复并测、各经独立对抗 review**：执行侧 13 条（B1/E1/E2/E3/C1/B2/P1/P2/P3/P4/R1/R2/R3）→ main 已 commit+push（9bfdde4..08827a4）；信号侧 1 条（S1）→ feature 分支（d4c866b，已 push）。执行侧全量 **702→724 passed**（净增 22 用例），信号侧 **501 passed**。开 `KILL_SWITCH` 前硬阻断（B1/E1/C1）+ 开 `QMT_SELL_PASS_LIVE` 前硬阻断（P1/P2）均已堵上。

**全量终审**（14 条落地后跨修复交互审）：6 个交互面 + 对抗核验 → **零跨修复冲突、零新回归、零修复不彻底**，仅 1 条轻微注释陈旧（committed_amount 方法级口径未列 PART_CANCELLED 终态，代码正确）已订正。14 条修复互不冲突、协同自洽。

doc/21（评审报告）+ doc/22（修复台账）**第六轮全部收尾**，待按 `CLAUDE.md` 整体归档。

### 状态
- ⚠️ **上线口径**：B1/E1/C1 应作为开 `KILL_SWITCH` 前硬阻断；P1/P2 应作为开 `QMT_SELL_PASS_LIVE` 前硬阻断。当前三道门控（`KILL_SWITCH`/`QMT_SELL_PASS_LIVE`/`QMT_AUCTION_TIMING_ENABLED`）默认关，暂遮住多数严重项的危害，但均为真实逻辑缺陷、开门控即暴露。

> doc/21（评审报告）+ doc/22（修复台账）为本轮处理文档；处理中暂留 `doc/` 根，全部处理完 + 全量 review 后按 `CLAUDE.md` 文档规则整体归档，本概要持久留存。

---

## 第七轮：双项目独立 + 联合交易整体评审（doc/24 / doc/25 / doc/26）

对执行侧 `qmt_strategy` 与信号侧 `stock-ah-premium-ai` 打板推送链分别做独立评审，再对两侧 HTTP 契约（watchlist 拉取 + qmt 回流）做联合整体评审。三份报告：[`24-执行侧qmt_strategy交易闭环独立评审报告`](../../../doc/已归档-完成不再维护/24-执行侧qmt_strategy交易闭环独立评审报告.md)、[`25-信号侧打板推送链独立评审报告`](../../../doc/已归档-完成不再维护/25-信号侧打板推送链独立评审报告.md)、[`26-两侧联合交易整体评审报告`](../../../doc/已归档-完成不再维护/26-两侧联合交易整体评审报告.md)。信号侧评审范围只限「服务于打板推送」的链路（watchlist 导出 + 选股 pipeline + 龙头打分 + go/no-go 回测闸 + qmt 回流 ingest + 内网鉴权），不含复盘/归因看板。

**结论**：确认问题去重后 34 条（执行侧 10 + 信号侧 17 + 联合 9，其中 E-2/J-2、E-5/J-7 为同一问题的跨侧两视角）、待确认 15、排除误报 40。

**红线（P0）两条**：
- **E-1**：写队列溢出丢弃发单前 PLANNED 关键落盘，而 `flush_confirm`/`is_healthy` 仍报成功 → 崩溃重启重复下单（限定「写线程慢但未死」子场景，无兜底）。
- **J-4/J-5/J-6**：回流四表（成交/委托/账户）NOT NULL 两侧口径不对称——执行侧脏数据合法落 None + 本地 SQLite 可空，信号侧 NOT NULL 拒收 500 → 该行 `synced=0` 永不收敛重试、对账恒 False、阻断次日开仓。

**P1 要点**：J-1（`continuation_prob` 信号侧是 tier 档位代表值、执行侧当绝对地板 0.3 用 → 首板/连板弱续档 FOCUS 票被四类可达策略首句结构性拒买；经确认为**口径错配待修**）；E-2/J-2（`LEADER_PULLBACK` 对真实 payload 死策略）；E-3（`QMT_ACCOUNT_LOSS_LIMIT` 配了 fail-open 永不触发）；E-4/E-6（间歇写失败漏检台账不一致 / 无 id 碎单去重两难）；E-5/J-7（分接口 token 启动日志明文泄露）；J-3（买入交易日历漂移仅告警不 fail-closed）；J-8（读/写 token 回落不对称致回流可恒 503，与第四轮 SIG-QMT-06 同源）；信号侧 S-1~S-7（同日换 pv 返回陈旧候选 / 缺 T 行用 T-1 冒充 / 炸板池 FAILED 当零炸板使退潮否决失效 / FOCUS 先验非法或降级静默落 NULL / 龙头降级 role_confidence 错配 / go-no-go 超额闸样本不等价）。

**两条待确认经与负责人确认定性**：原 U-4（独苗题材即板块龙头）→ **缺陷**，上移为 S-17；原 U-7（回测把买入后卖不出的票按 ≤0 计入 GO 闸样本）→ **有意的保守 survivorship 口径**、非缺陷，已移出。

**贯穿性根因**：① 数据缺口/接口失败「静默退化为 0/旧值/None」贯穿两侧（信号侧坏值透传 + 回流毒丸）；② 概率/角色/策略语义跨两仓不可比（缺契约语义对齐）；③ token 与交易日历口径依赖运维纪律而非代码自洽。

### 修复进度（2026-06-20，逐项决策见 [`27-修复顺序指南`](../../../doc/已归档-完成不再维护/27-修复顺序指南.md) §3）

按 [`27`](../../../doc/已归档-完成不再维护/27-修复顺序指南.md) 分层推进，**已实现 15 项 + 1 迁移、各带单测、已提交推送**（两个 P0 均已堵）：

- **执行侧单仓批次**（main `0f06fc6`，全量测试绿）：E-1 写队列溢出 fail-closed（**P0** 重复下单窗口）、E-4 健康位 sticky 门闩、E-5 分接口 token 脱敏、E-7 配 0 不被吞、E-8 非法配置 fail-closed、E-9 `TradeCalendar` Protocol 补 `trading_days_left`、E-10 卖出关时不污染行情健康位、J-2 删除龙回头死策略（`leader_pullback.py` 已删）。E-3 经复核已被强制回撤闸（`assert_safe_to_trade` 必配 `account_drawdown_limit`）兜住、保留告警，无需改码。
- **信号侧单仓批次**（feature `110888c`，509 passed）：S-1 缺省版本取最新 READY 报告 pv 防陈旧候选、S-6 龙头降级回写 `role_confidence`、S-11 `highest_chain` 未知不误判冰点、S-15/S-16 非 ASCII token 按字节比较返 401（非 500）、S-17 独苗题材不判板块龙头。
- **J-4/5/6 回流毒丸**（feature `e7a9e3f`，迁移 `0061`，511 passed）：**P0**——放宽 `qmt_trade/qmt_order/qmt_account_daily` 易脏列 NOT NULL + 加 `ingest_data_quality` 缺测留痕列；脏回报可落库、不再 500 致 `synced=0` 死循环。**生产需执行 `alembic upgrade head`（0060→0061）。**
- **缺测改造**（S-2/S-3/S-9/S-10/1.2.0 取数 + 缺测 sentinel 契约 + 执行侧买入拦截/持仓卖出 + E-6 + J-3 日历接口 + J-1/J-8/S-12）：因属**跨两仓、改动实盘买/卖决策**的大特性，整理为自包含实现规格 [`29-缺测改造-设计与实现规格`](../../../doc/已归档-完成不再维护/29-缺测改造-设计与实现规格.md)（main `5babb3b`），**留待专门一轮独立执行**。
- **回测口径类**（S-7/S-13/S-14/U-8）：移交 [`28-回测方向问题记录`](28-回测方向问题记录.md)，代码修复优先级低、暂不处理。

### 状态
- 两个 P0（E-1 重复下单、J-4/5/6 回流毒丸）**已修复**；两侧单仓批次全清。剩余 = 缺测改造大特性（见 doc/29）+ J-1/J-8/S-12 收尾。
- 真机量纲类待确认（封流比 `bidVol`、竞价量能比 `tick.volume`、`traded_time` 秒/毫秒、`lastClose` 缺帧）随上线实测核，详见各报告「待确认」。
- 部署侧待执行：信号侧 `alembic upgrade head`（含 0061）；缺测改造落地时另有新迁移 + J-8 须同步改运维手册 token 口径。

> doc/24/25/26（评审）+ doc/27/28/29（修复指南/回测/缺测规格）为本轮处理文档；处理中暂留 `doc/` 根，全部修复收尾后按 `CLAUDE.md` 文档规则整体归档，本概要持久留存。

### 缺测改造大特性实现（2026-06-21，规格见 [`29`](../../../doc/已归档-完成不再维护/29-缺测改造-设计与实现规格.md)）

doc/29 阶段 A–E **全部落地、各带单测、分子项提交**（信号侧 `feature/limit-up-watchlist-signal`、执行侧 `main`，未 push）；两侧全量绿（信号 533 passed/1 skipped、执行 759 passed）。

- **阶段 A 信号侧产出端**：A1 watchlist 契约升 1.3.0 + `tradable_flag=DATA_MISSING` 枚举 + 落表 `data_missing_reason` 列（迁移 0062）；A2 炸板池/跌停池 FAILED 区分「未知 vs 真空」、未知按市场级保守否决（不当零炸板放行）；A3 信号日 T 行缺失不用 T-1 冒充；A4 收益按市场交易日跨度校验；A5 连板高度切 `limit_list_d.limit_times` 主源；A6 封板时序切 `limit_list_d`（修 `open_times` 恒空）；A7 落表收口：任一§2 核心交易指标缺测 → `DATA_MISSING` + 原因留痕，**接口级缺测护栏**（`limit_list_d` 整体故障不级联全表强卖）。
- **阶段 B 执行侧消费**：B1 契约扩 `data_missing(+reason)` 全链透传（含 SQLite round-trip）；B2 `buy_prefilter` 缺测硬禁买 + loader/entry_router/order_executor 三层冗余 + `prior_gate_reason` fail-closed；~~B3 持仓缺测强制清仓~~ **（B3 已于 2026-06-21 下线，见下方口径变更）**。
- **阶段 C**：E-6 以券商 `query_stock_orders.traded_volume` 权威收口本地 `filled_volume`；J-4/5/6 执行侧回报归一脏行源头告警。
- **阶段 D（J-3）**：信号侧新增 `/api/internal/trade_calendar` 接口；执行侧盘前校验日历覆盖度、不足补取并入共享日历+落盘，补取失败且仍不足 → fail-closed 只守仓不开新仓。
- **阶段 E**：J-1 续板阈值显式中/低档分界、改 `settings.prior_continuation_min` 可配（买卖行为不变）；J-8 通用 token 打通所有接口（ingest 写 token 回落 watchlist 通用 token，**有意回退 SIG-QMT-06**，同步改 README/待办/运维手册）；S-12 龙头打分 pctl 基准也注入 cyq、消除筹码口径分叉。
- **对抗式评审（A / B / 整体三轮）**：
  - A、B 各一轮多视角对抗复核——**B 修两处仅生产路径断链**（`run.py` 唯一 SignalPrior 装配点漏传 `data_missing` 致 B3 实盘为死代码；watchlist SQLite 落表丢 `data_missing` 列致 round-trip 抹平），均补回归单测堵注入桩盲区；A 修 `open_times` float 误判缺测/昨日 `limit_list_d` 未合并致晋级率漂移/契约漏 WATCH。
  - **整体 review（C/D/E + 跨两仓端到端）确认 9 项、已逐条处理**：E-6 收口改【单调向上、绝不下修】（消除重连 stale 快照/重启把已成 filled 下修→欠计→过度承诺/漏卖的危险方向）+ 重启按明细重算取 max 不撤销收口；S-12 **回退**（pctl 注入 cyq 会因 cyq 仅入选可得而造成 apples-to-oranges，保持 pctl 不注入 cyq）；J-1 续板下限消费侧 `or`→is-not-None（显式配 0 能真正关闸）；D 日历 fail-closed 日志措辞对齐 engine 实际门控 + 落盘只整体覆盖写不截断。
  - **整体 review 余 2 项「待确认」+ `fd_amount` 死代码（P1-2 存量）落 [`30`](../../../doc/已归档-完成不再维护/30-缺测改造遗留与待确认项.md)，已于 2026-06-21 处理完（见下方口径变更）。**

### 缺测口径变更 + doc/30 三项处理（2026-06-21，详见 [`30`](../../../doc/已归档-完成不再维护/30-缺测改造遗留与待确认项.md) 文末「处理结果」）

- **口径变更（负责人决策）· 下线「缺测持仓强卖」(B3)**：卖出完全交由执行侧 xtdata 实时盘口扳机（炸板/破位/烂板/止损等）裁决，不再因信号侧缺测标记强清持仓；缺测**仅保留买入侧拦截**(B2)。执行侧移除 `SignalPrior.data_missing`、`sell_decider`/`main` 两处+无盘口强清分支、`prior_provider` 透传与全部 B3 卖出单测；买入侧（`buy_prefilter`/`entry_router`/落表 round-trip）保留。**doc/30 项 1（导出坏行绕过缺测强卖）随 B3 下线自动消解**，无需改导出契约。执行侧全量 **753 passed**（759 − 6 个 B3 卖出用例）。
- **doc/30 项 3 · `fd_amount` 回退死代码修复并激活**：`_compact_stock_row` 封单额回退由顶层 `row.get("fd_amount")`（恒 None）改读已合并的嵌套键 `lld.get("fd_amount")`；修正原假阳性单测使其走真实 `_merge_limit_list_d_into_rows` 合并路径 + 加回归护栏。属买入侧行为变更（影响 `seal_ratio_pct`→选股）。信号侧全量 **533 passed/1 skipped**。
- **doc/30 项 2 · 生产历史数据缺测率核实（只读）**：生产数据陈旧（schema ≤1.1.0，1.3.0 未部署），高 `float_mktcap` 缺测系**列后加未回填的迁移伪缺测**（已坐实）；`limit_order` 全表 0% 缺测、`seal_ratio_pct` 缺测从不由分子驱动 → **项 3 对历史缺测率零影响**。现有数据无法测 1.3.0 真实缺测率，**需 1.3.0 部署后实跑若干交易日再复测**；复测前不对封流比类加去重/降权/护栏（无证据支撑改码）。

**部署侧新增待执行**：信号侧除 0061 外需再跑 **0062** 迁移（`data_missing_reason` 列）；J-8 后运维可只配一套 watchlist 通用 token。缺测改造 1.3.0 部署后按 doc/30 项 2 复测真实缺测率。

---

## 第八轮 · 三侧联合交易复评与修复（2026-06-22，评审 [`31`](31-三侧联合交易代码评审-20260621.md)，台账 [`32`](32-三侧评审修复台账与待决策清单.md)）

对「信号侧打板推送 / 执行侧 / 两系统联合」分三部分逐条对代码复评，共记 57 条（剔误报后）；按「能确定性修则修（带单测）、需真机/负责人/回测口径则不做默认处理只登记」处理。两侧全量绿（信号 542 passed/1 skipped、执行 764 passed）。

- **核心口径纠偏（缺测走偏）**：LLM 软先验 `continuation_prob` 此前被误纳入「核心交易指标缺测集」，致大模型一次解析失败/漏写/近义词就把行情数据齐全的好票(甚至整档)误判缺测弃买，背离「某票缺重要数据才不买它」的本意。已把 `continuation_prob` 移出核心缺测集（缺它=软降级 None、该票仍可买、执行侧按盘口 fail-open）+ 先验结构防御（坏结构不崩整表）。（信号-1/4/5/6）
- **已修复（带单测）**：信号侧 12 项（1/2/4/5/6/7/12/13/14/15/19/20）、执行侧 8 项（1/2/4/7/8/18/19/21）、联合 9 项（2/3/4/5/6/7/8/11/14）。要点：执行侧卖出只认实时盘口(去 fail_conditions 关键词误触发)、失败卖单不占下单配额、持仓回写失败 fail-closed 重建、瞬时写抖动不再永久冻结开仓；联合侧 NaN 强度分拒绝、坏信封/全批失败判真失败重试、买入日不一致 fail-closed、复盘多接口不再因脏行/空值整页 500、复盘生效版本与导出同序。两仓「缺测强卖」陈旧注释全部纠正。
- **未做默认处理、登记待决策（台账 32 第二部分）**：① 真机实测前置——下单/状态魔数 23/24/11（执行-5/6）、弱封减仓定价（执行-9）、竞价 volume 单位与 9:25 后行为（执行-11、两系-10）；② 跨两仓契约/口径需负责人定——盘中实时 ST 复核取数（执行-3）、回流脏行 NOT NULL 放宽/死信（两系-1/9）、净值出入金口径（两系-12）、下单成功率口径（两系-13）；③ 需并发/基础设施设计——并发生成落选股序列化(信号-3)、推送 at-least-once 判重(信号-21)、updated_at 时间基准统一(信号-22)；④ 对账告警口径(执行-13/14/15/16/20，仅告警不阻断)；⑤ 回测口径 6 项登记进 [`28`](28-回测方向问题记录.md)（信号-8/9/10/11 对应 U-8/S-13/S-14/S-7 已在册，新增 S-15/S-16 = 信号-17/18）。

> doc/31（评审）+ doc/32（修复台账）为本轮处理文档，处理中暂留 `doc/` 根；待决策项闭环后按 `CLAUDE.md` 文档规则整体归档，本概要持久留存。
