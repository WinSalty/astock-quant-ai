# 12 · 第二轮评审（doc/11）修复台账

> 处理对象：[`11-量化交易代码第二轮独立评审报告.md`](11-量化交易代码第二轮独立评审报告.md)（92 条：6 P0 / 25 P1 / 39 P2 / 22 P3）。
> 本台账记录：① 逐条独立验证结论（是否真实、是否在修复范围内）；② 分轮修复（阶段 A–E）的规划/开发/review/测试/提交进度；③ 处理过程中**自行拍板的口径决策**（供事后补充意见）。
> 按工作区文档约束：不含工时/周期预估，只按阶段+验收+依赖顺序组织。

## 0. 修复方法与轮次划分

- **先验证再修复**：每条发现都重新打开真实代码核对（不轻信原报告行号，按符号定位），判定 `confirmed / partial / refuted` 与 `in_scope`（仅因缺 miniQMT 真机才无法验证的占位=out_of_scope，纯逻辑错误仍 in_scope）。
- **按系统性根因成批修复**，对齐报告 §四阶段：
  - **Round 1 = 阶段A**：资金安全与状态可恢复（主题1+2）。
  - **Round 2 = 阶段B**：风控护栏与价位口径真正生效（主题3+5）。
  - **Round 3 = 阶段C**：回测裁决口径与龙头打分可信（主题6+7）。
  - **Round 4 = 阶段D**：供数与建仓闭环（主题4+8）。
  - **Round 5 = 阶段E**：复盘/回流/并发/鉴权健壮性（主题9+10+11+余 P3）。
- **每轮流程**：分析 → 规划 → 开发（补中文注释）→ review → 测试 → 修复 → review → 测试 → 提交。两仓分别提交（执行侧 `astock-quant-ai` main、信号侧 `stock-ah-premium-ai` feature 分支）。
- **测试基线**：执行侧 `pytest` 449 passed；信号侧评审相关 68 passed（修复前已记录，作为回归对照）。

## 1. 待补充确认的口径决策（处理中自行拍板，事后请补充意见）

> 这些点报告未给唯一解或涉及业务口径，我按“资金安全优先 / 保守”原则先行决定并落地，列此供复核。

| # | 决策点 | 我的处理 | 可调整方向 |
|---|---|---|---|
| D1 | 持久化写线程故障的 fail-closed 粒度 | 写线程死亡/关键落盘失败 → 置“存储不健康”标志，**停开新仓 + 强告警**，但**不**冻结卖出（保留风险出场能力） | 若要求绝对保守可升级为全局 kill |
| D2 | 重启后持仓状态机重建数据源 | 启动时从 **本机 SQLite 当日 CLOSE/INTRADAY 持仓快照 + 本地台账成交** 重建 `PositionUnit`；运行期再用 QMT `apply_position_snapshot` 校准可卖量 | 若目标机可稳定 query 持仓，可改为以 QMT 实时快照为唯一权威 |
| D3 | 卖单卡死复位口径 | 卖单“同步失败/拒单/全撤零成交/挂不到价超时”四类 → `revert_selling` 回 HOLDING/PART_SOLD，下一轮重挂；卖单加独立 TTL | — |
| D4 | ST 票涨停价 5% | 信号侧补 `limit_up_price` + `is_st`/`board` 落契约并透传；执行侧据此用 5% 或直接按 MISSING 转观察（缺信号价时不再按 10% 兜底） | — |
| D5 | 回测 go/no-go 改净收益口径 | 强制含费（往返成本），`MIN_MEAN` 设为覆盖成本的正阈值；卖出侧加涨停封板顺延；未平仓样本保守计入；加最小样本/显著性 | 阈值具体数值待信号侧实测校准 |
| D6 | ST 5% / 价位契约（#5#18#63） | 不给信号侧加 `limit_up_price` 落表（源表无列，需迁移+重复涨停价逻辑）；改在**执行侧**：①透传契约已有的 `name`（#63 的核心是 ST 识别，板块本可由代码前缀可靠推断）；②board_rules 据 `is_st_name(name)` 对主板 ST 用 5%、创业板 ST 仍 20%，算出正确涨停价；③watchlist_loader 据 name 做 ST live 过滤（#10）。`reasonable_open` 合理高开区间仍由执行侧 board_rules 兜底（+2%~涨停），真实策略带的高开区间留作信号侧后续增强 | 若要信号侧权威涨停价，可后续加迁移+落表 |
| D7 | 对账不通过阻断次日开仓（#9） | 对账检出漏单/串单/资产偏差 → 置持久"对账未通过"标记（本机 SQLite `system_flags` kv 表），次日 prewarm 读到即 `open_new_position_allowed=False`(SELL_ONLY)，人工清标前不开新仓；needs_backfill 立即触发补采+持仓重建 | 人工清标方式：删 flag 行 |
| D8 | 账户回撤查询失败 fail-closed（#70） | 有日初基线但当前资产查询失败→无法核验回撤→**禁开新仓**（不冻结卖出）；非全局 FREEZE，避免一次抖动停掉风险出场 | — |
| D9 | 封单不稳护栏 seal_ratio_min（#41） | 改为**可配置**（原硬编码 0.0=死）；**默认 0=关闭**——执行侧封流比依赖 bidVol 的"手/股"量纲(auction_factors)，目标机实测确认量纲并标定阈值前不默认激活，避免误杀真金买入路径上的强板；实测后用 `QMT_SEAL_RATIO_MIN` 配正阈值启用 | 实测确认 bidVol 量纲后可改默认值 |
| D10 | 龙回头 LEADER_PULLBACK 可达性（#42） | **不强行改路由**：现路由「高位低吸→DIP_BUY_MA」是既定且有测试的设计；龙回头是预留给"龙头+高位回踩/分歧"显式信号的策略，当前信号侧(DABAN/BANLU/DIXI)尚不产出该信号类型→记为待信号侧增设龙回头战法分类（属战法口径决策，非纯 bug）；策略本身已注册+单测，非死代码 | 信号侧增 龙回头 setup/family 后即可达 |
| D11 | 转次优 next_best（#68） | `next_best` 链路当前为空(entry_router 不构造次优序列)，`try_next_best` 优雅放弃(不追高)；填充 next_best 需引入"当日候选次优排序"的新机制，属增量功能而非 bug → 记入待办，当前以"买不进即放弃该票"为安全默认 | 后续可由 entry_router 构造 next_best 序列 |

（后续每轮新增决策点会续写到此表。）

---

## 4. Round 2 = 阶段B：风控护栏与价位口径

### 4.1 分析（直接读码核对）
| # | 结论 | 根因 |
|---|---|---|
| 14 P1 | ✅ | `app/main.py` `on_reconnect_backfill` 把 `_trade_conn_ok` 置 False 后 `finally` 立即置 True，同一同步调用内 FREEZE 永不触发 |
| 15 P1 | ✅ | `_market_feed_ok` 初始化 True，全工程无置 False 处 → 行情断流冻结分支死代码 |
| 33 P2 | ✅ | `config/settings.py:206` `_as_decimal(...) or Decimal('1.0')`，配 0 被静默改满仓 |
| 39 P2 | ✅ | `_open_blocked_by_risk`/`run_sell_pass` 调 gate 时 `unit_float_loss` 恒 None → 单票止损死 |
| 67 P2 | ✅ | `price_deviation_guard_pct` 解析后无任何引用，限价无合法性/偏离校验直发券商 |
| 70 P2 | ✅ | `_account_drawdown` 资产查询失败返 None → gate 不冻结（账户级止损闸 fail-open） |
| 74 P3 | ✅ | risk 文档称账户击穿"冻结买入与卖出"，实现只冻买入（run_sell_pass 不喂 account_drawdown） |
| 10 P1 | ✅ | `universe_filter.is_tradable_universe`(ST/停牌/退市) 全链路无调用；loader 只用前缀白名单 |
| 9 P1 | ✅ | `close_batch` 跑完 reconcile 仅 log，不阻断次日开仓、不触发补采 |
| 34/35/36 P2 | ✅ | 资产对账仅"方向相反"才报（同向漏采不报）；卖单 remark `SELL\|` 弱关联只认 `LUP\|`；弱关联不比对买卖方向 |
| 5 P0 | 🟡 | 契约确有 `close/board/name`、无 `limit_up_price`/合理高开区间；执行侧 remote_watchlist 丢弃 board/name；建仓限价对低吸用 reasonable_open_low(+2% 占位) → 见 D6 |
| 18 P1 | ✅ | board_rules 对主板 ST 仍按 10% 现算涨停价（缺 is_st）→ 超 ST 法定涨停价废单/漏买真5%顶板 |
| 45 P2 | ✅ | `board_rules.budget_prices` signal_close<=0 时现算涨停价 0 并标 LOCAL_CALC 进可交易名单（应 MISSING） |
| 63 P2 | 🟡 | 契约 board/name 被执行侧丢弃；board 本可由前缀可靠推断，核心缺口是 ST 识别（name）→ 见 D6 |

### 4.2 规划
风控五闸真生效（#14#15#39#67#70）→ 价位口径（#5#18#45#63）→ ST 过滤（#10）→ 对账阻断+补采（#9）+ 对账correctness（#34#35#36）→ 配置（#33）+ 文档（#74）。

### 4.3 开发 / review / 测试 / 提交

**开发（均补中文注释）**
- `app/main.py`：开仓总闸加存储/对账/账户回撤 fail-closed（#2#9#70）；`_evaluate_and_sell_unit`+单票止损 `_unit_stop_loss_breached`（#39）；`_limit_price_sane` 限价合法性/偏离闸（#67）；`report_market_feed`+prewarm 重置（#15）；`on_reconnect_backfill` 解冻改"补采成功才解冻"（#14）；`close_batch` 对账阻断+补采+重跑（#9）；`_set/_read_reconcile_block`。
- `risk/risk.py`：账户击穿"冻买不冻卖"口径文档澄清（#74）。`config/settings.py`：target_position_ratio 0/负口径（#33）。
- `common/board_rules.py`：主板 ST 5%/创业板 ST 仍 20%、signal_close<=0→MISSING（#18#45）。`watchlist_loader.py`：ST live 过滤（#10）。`remote_watchlist.py`+`contracts/models.py`+`mappers.py`+`schema.py`：透传 `name`（#63）+ watchlist 表 name 列迁移。
- `reconcile/reconcile.py`：资产同向超额偏差告警（#34）、卖单按 ts_code+方向弱关联（#35）、强/弱关联校验买卖方向（#36）、回报独占防一对多（复审 P2-1）。
- `storage/{schema.py,sqlite_repository.py}`+`data_writer/repository.py`：`system_flags` kv 表 + set_flag/get_flag（#9）。`auction/auction_poller.py`：feed_health_sink（#15）。

**review（独立审计 agent）**：确认 P0/P1 主修闭环（ST 5% 口径、回撤 fail-closed 无粘滞、解冻条件、无循环 import）；查出并已修：
- 复审 P1-1：close_batch 用补采前 report 判定阻断、且会把良性 order_failed/可补采项误判 → 改为补采后重跑对账 + 只对真异常（missing_report/manual_order/残留 trade 不勾稽/资产偏差）阻断。
- 复审 P1-2：单票止损/卖出未防 last_price<=0（坏 tick 凭空强平+0 价废单）→ 止损与下单价均加 `>0` 防御。
- 复审 P2：卖单弱关联一对多（加回报独占）、set_flag 无 busy_timeout（加 5s）、target_position_ratio 注释失真/无基线绕过（负值钳 0 + ratio<=0 确定性不开仓）。

**测试**：新增 `tests/test_round2_stage_b.py` 共 25 例。全量 **496 passed**（阶段A 后 471 → +25）。

**遗留（记入待办，非本轮可闭合）**：#15 行情断流冻结目前只在竞价段(AuctionPoller)上报；盘中连续交易段需待 `sell_books_provider`(实测) 落地后把盘口取数失败接到 `report_market_feed(False)`。`reasonable_open` 合理高开区间仍执行侧 +2%~涨停兜底，真实策略带高开区间留作信号侧后续增强。

**提交**：执行侧 main 分支。

---

## 5. Round 3 = 阶段C：回测裁决口径与龙头打分可信（信号侧 stock-ah-premium-ai）

### 5.1 分析（直接读码核对，全 confirmed）
| # | 结论 | 根因 |
|---|---|---|
| 3 P0 | ✅ | `limit_up_push_service.score_stocks([tiered])` 只用已入选小子集建分位/分布 → leader_strength_score 系统性失真，直污染执行侧资金加权/开仓 top-N |
| 21 P1 | ✅ | `build_scoring_context` 的 max_board_level/theme_max_board 同基于小子集 → 板块龙头/题材梯队子分错误 |
| 49 P2 | ✅ | `_as_float` 接受 NaN/Inf → 毒化分布排序、封流比/量比分母 |
| 50 P2 | ✅ | `_first_limit_seconds` 不校验交易时段，盘前竞价(<9:30)/脏时间得负秒数被当"最早最强"抬满封板质量子分 |
| 4 P0 | ✅ | `_backtest_one` 卖出日只防跌停封死顺延、不防一字涨停封死 → "卖不出却记涨停价"的最优样本计为高收益 |
| 22 P1 | ✅ | 买入后无卖出行情样本 `tradable_flag=0` 移出收益分布 → 剔最差尾部（survivorship） |
| 23 P1 | ✅ | go/no-go 用毛收益且默认不含费 → 扣费后净亏的策略仍 GO |
| 24 P1 | ✅ | 对照组隔日收益固定 B→T+2 open，与可配置 hold_window/sell_price_policy 不一致 → 超额错配 |
| 51 P2 | 🟡 | 一字依赖的 limit_type『一字』标签永不命中（存 10cm/20cm）；但价格判据(_is_flat+开盘贴涨停)实际覆盖一字，故功能未漏，标签为冗余死分支（记录，不改行为） |
| 52 P2 | 🟡 | 对照池取 LLM 上下文前 360 只 + 无行情样本静默丢 → 截断+幸存者偏差。属供数层限制（全池未持久化超 360），记入待办增强 |
| 53 P2 | ✅ | go/no-go 仅点估计阈值、无显著性，30 样本即放行 |

### 5.2-5.3 开发 / review / 测试 / 提交
**开发**
- `limit_up_leader_scoring_service.py`：`_as_float` 拒 NaN/Inf(#49)；`_first_limit_seconds` 交易时段校验+竞价钳 0(#50)；`_pct_rank` 最小样本中性兜底(#3)；`score_stocks` 加 `context_rows`(全涨停池)解耦分布/分位基准与落表对象 + `_raw_strength` 抽取(#3#21)。
- `limit_up_push_service.py`：`score_stocks(context_rows=全涨停池)`。
- `limit_up_backtest_service.py`：`_is_flat_limit_up` 卖出日涨停封死顺延(#4)；q_sell None/顺延触顶仍封死 → 保守 mark-to-market `min(0,…)` 计入分布(#22)；`evaluate_go_no_go` 改净收益(强制扣往返费)+t 统计量显著性(#23#53)；`backfill_market_pool`/`_pool_next_day_return` 随 hold_window/sell_price_policy(#24)。

**review（独立审计 agent）**：查出并已修：
- 复审 P0-1（stop-ship）：全池基准接线断裂——`pipeline.get("limit_up_stocks")` 恒 None、静默回退入选子集，#3 修复在生产路径不生效 → 改读 `context.get("limit_up_stocks")`。
- 复审 P0-2/P1：全池(无 selection)与入选票(含 LLM selection)强度不同量纲、分位被抬顶 → 分位基准与被查询值【一律剥 selection】同量纲比较；展示用 leader_strength_score 仍用完整行。
- 复审 P1-1：#24 对照组口径未透传、库内无口径列 → `_control_source` 按 hold_window/sell_price_policy 自动命名空间化 source（读写同 cfg 派生；口径不一致读空→超额闸门判不通过而非静默用错口径）+ 回填脚本 CLI 透传。
- 复审 P2-1：顺延触顶仍封死仍按封板价记成交 → 改走保守 mark；P2-2：`_is_flat_limit_up` 校验真贴涨停幅度/价。

**测试**：新增 `tests/test_round3_stage_c.py`（含全池分位、selection 不抬分位、净收益+显著性、涨停封死判据）+ 扩 go/no-go 测试。信号侧相关 **140 passed**。

**遗留（记入待办）**：#51 limit_type『一字』标签为冗余死分支（功能由价格判据覆盖，不改）；#52 对照池 360 截断+无行情样本丢弃属供数层限制（全池未持久化超 360），列后续增强。

**提交**：信号侧 `feature/limit-up-watchlist-signal` 分支。

---

## 6. Round 4 = 阶段D：供数与建仓闭环

### 6.1 分析（直接读码核对）
| # | 结论 | 根因 |
|---|---|---|
| 16 P1 | ✅ | 竞价段 BUY 锁幂等后被编排层(竞价择时默认关)丢弃、无 OPENING 补单入口 → 最强龙头默认配置买不进 |
| 17 P1 | ✅ | chase_auction_strong 买分支硬编码 order_phase=AUCTION，定盘段(9:25-9:30)仍按竞价单提交→TTL 立即过期废单 |
| 41 P2 | ✅ | chase_limit_up 封单不稳护栏 `_SEAL_RATIO_MIN=0.0`，`ratio<0` 恒 false=死代码 |
| 42 P2 | 🟡 | LEADER_PULLBACK 路由依赖 family 含"龙回头"/"龙"，真实 family 仅 DABAN/BANLU/DIXI→不可达（见 D10） |
| 43 P2 | ✅ | 半路族(BANLU)"首板半路"因 setup 含"首板"被误路由到竞价强开追 |
| 68 P2 | 🟡 | next_best 永不赋值，转次优是死代码（见 D11，优雅放弃，非资金风险） |
| 26/27 P1 | ✅ | first_board_vol 源是 float，落表用 `_int_or_none` 恒判 None→竞价量能比分母每票每日缺失 |
| 44 P2 | ✅ | XtdataTickSource.get_full_tick 前从不 subscribe_quote |
| 58 P2 | ✅ | watchlist 导出单行校验失败静默跳过，无声漏标的 |
| 85 P3 | ✅ | 缺省 prompt_version 取"最新 READY 报告"，与已落选股行 pv 可能不一致→整日空集 |

### 6.2-6.3 开发 / review / 测试 / 提交
**开发（执行侧）**：base.order_phase_for(定盘前 AUCTION/定盘后 OPENING) + chase_auction_strong/chase_limit_up 改用之(#17)；EntryRouter.release + 编排层竞价择时关时解锁(#16)；OPENING 买单开盘前 TTL 顺延至 9:30(#16/#17)；is_daban 守卫半路首板不误路由竞价强开(#43)；seal_ratio_min 可配置(#41)；tick_source 取数前 subscribe_quote(#44)；remote_watchlist skipped_count 告警(#58)。
**开发（信号侧）**：`_vol_to_int` float→int 落 first_board_vol(#26/#27)；watchlist 响应 skipped_count + 坏行 error 告警(#58)；缺省 prompt_version 优先取已落选股表最新批次(#85)；回填脚本透传 hold_window/sell_price_policy。

**review（独立审计 agent）**：查出并已修：
- 复审 P1-1：`QMT_SEAL_RATIO_MIN=0` 被 `or 默认` 当假值覆盖、护栏关不掉 → 改 is-not-None 守卫。
- 复审 P2-1：seal_ratio_min=0.005 建立在未校验 bidVol 量纲的封流比上、激活会误杀强板 → 默认改 0(关闭，可配置)，量纲实测后启用（见 D9）。
- 复审 P2-2：竞价择时关时每帧重复路由/采集洪泛 decision_log/日志 → EntryRouter._record 按 action 变化去抖 + 编排层每票每日只采集一次。
- 复审 P2-3：OPENING TTL 顺延对卖单(默认 OPENING)误生效会抵消 #12 卖单 TTL → extend_to_open 参数仅买单顺延。
- 经核对无问题：#43 路由对真实值域穷举正确、#44 订阅、#85 max(id)取批次、#26/#27 取整口径。

**测试**：执行侧 `tests/test_round4_stage_d.py`（7 例）→ 503 passed；信号侧 `tests/test_round4_stage_d.py`（2 例）+ 既有 72 passed。

**提交**：执行侧 main + 信号侧 feature 分支。

---

## 2. 逐条验证结论（Round 0）

> 状态随 21 个模块验证 agent 的结论 + 我本人直接读码核对回填。`real` 列：✅ 确认 / 🟡 部分 / ❌ 误报；`scope` 列：✅ 本次修 / ⏭️ 仅真机可验证。

（详见各轮“分析”小节；汇总表在 Round 0 完成后回填。）

---

## 3. Round 1 = 阶段A：资金安全与状态可恢复

### 3.1 分析（直接读码核对，全部 confirmed）

| # | 结论 | 根因（file:line） |
|---|---|---|
| 1 P0 | ✅ | `storage/write_queue.py:122-132` 写任务 commit 抛错只 rollback+log，`finally` 仍 `task_done()`；`flush()` 仅看 `unfinished_tasks==0` → `flush_pending` 对落盘失败返 True，发单前落盘保证失效 |
| 2 P0 | ✅ | `storage/local_stack.py:46` 构造 `AsyncWriteQueue` 未传 `on_failure`；`is_healthy()` 全工程无调用 → 写线程运行期死亡后 `submit` dead-drop 静默丢弃、交易继续（fail-open） |
| 6 P0 | ✅ | `position/position_manager.py:58` `_units` 纯内存；`app/run.py`/`main.py` 启动无任何 `load_positions`；重启后隔夜 T+1 持仓永不进卖出决策 |
| 7 P1 | ✅ | `order/order_executor.py:394` 发单成功后 `order_id` 仅 `update`（异步镜像），未同步落盘 → 崩溃窗口券商有单/台账只认 PLANNED 的孤儿单 |
| 28 P1 | ✅ | `order_executor.py:80,84,397,399` `_ttl_deadline`/`_decision_by_biz` 纯内存，重启不重建 → 崩溃前 SUBMITTED 在途单永不再被 sweep 撤 |
| 29/38 P1/P2 | ✅ | `position_manager.py:333` `apply_position_snapshot` 全工程无调用点（死代码），T+1 可卖量纯本地推算 |
| 30 P1 | ✅ | `data_writer/snapshot_job.py:112-146` 补采只 `data_writer.upsert_*`，不回写持仓/台账 → 断线期间成交漏卖 |
| 32/40/69 P2 | ✅ | `order_executor.py:88` `_orders_count_by_date` 纯内存，重启清零 → 单日下单次数硬闸被绕过 |
| 62 P2 | ✅ | `data_writer/callbacks.py:116-130` `on_stock_trade` 先 `upsert_trade`（可抛）再 `add_fill`/`position_sink` → 落库异常致成交从台账与持仓双双丢失 |
| 19 P1 | ✅ | `app/scheduler.py:110` 盘中重启 PREWARM 重跑 → `main.py:242` `_capture_day_open_equity` 把当前(已亏)资产当日初基线，回撤熔断失效 |
| 11 P1 | ✅ | `order_executor.py:510-519` 卖单同步失败仍返回 `biz_no` → `main.py:611-613` 据非 None 调 `mark_selling` 置 SELLING，无复位路径永久卡死 |
| 12 P1 | ✅ | 卖单无 TTL/撤改：`place_sell` 不登记 `_ttl_deadline`，`sweep_expired` 也只处理买单语义；挂不到价全天卡 SELLING |
| 13 P1 | ✅ | `position_manager.py:151-157` 再买入命中 SOLD 单元：`else` 分支并入旧单元，且重锁条件把 SOLD 排除 → 状态停 SOLD，新仓永不可卖 |
| 31 P1 | ✅ | 卖单被拒/全撤零成交无 `revert_selling` 钩子（`callbacks.py` 卖单终态不复位持仓），当日再不可卖 |

附带同源问题：#8（SUBMITTED 到期被 sweep 标记已处理并删 TTL 却从不撤单，`order_executor.py:631-696`，✅）、#92（committed_amount 对部成/已成仍全额计入，✅ 并入本轮一起修）。

### 3.2 规划（实现顺序，自底向上）
1. 写队列 fail-closed：`flush_confirm`（区分落盘成功/失败）+ `on_failure`/`is_healthy` 接线（#1#2）。
2. 持仓/台账重启重建：本地快照表 + 台账成交重建 `PositionUnit`；`apply_position_snapshot` 接线；补采回写持仓/台账（#6#29#30#38）。
3. order_executor 内存态重建：`order_id` 发单后同步落盘；`_ttl_deadline`/`_decision_by_biz`/`_orders_count_by_date` 重建；SUBMITTED 到期真正撤单（#7#8#28#32#40#69）。
4. 卖出状态机复位：`revert_selling` + 卖单 TTL + 同步失败不置 SELLING + 再买入命中 SOLD 开新单元（#11#12#13#31）。
5. 回调顺序 + 回撤基线持久化（#62#19）。

### 3.3 开发 / review / 测试 / 提交

**开发（执行侧 qmt_strategy，均补中文注释）**
- `storage/write_queue.py`：新增 `flush_confirm`（屏障任务 + `_batch_ok` 区分"已 drain"与"已持久 commit 成功"）、`set_on_failure`、commit 失败置标志（#1#2）。
- `storage/sqlite_ledger.py`：`flush_pending` 改走 `flush_confirm`（#1）。`storage/local_stack.py`：`set_on_failure`/`is_healthy` 接线（#2）。
- `position/position_manager.py`：`rebuild_from_broker_positions`（QMT 权威重建/校准）、`revert_selling`、再买入命中 SOLD 开新单元、`_units` 结构性写入/迭代加 RLock（#6#13#29#30#38；并把 #61 的崩溃面提前堵）。
- `order/order_executor.py`：`rebuild_runtime_state`（TTL+下单次数重建）、发单后同步落 order_id、TTL 撤 SUBMITTED、`place_sell` 同步失败返 None+登记卖单 TTL、`committed_amount` 已成+剩余口径（#7#8#11#12#28#32#40#69#92）。
- `app/main.py`：`on_storage_failure`/存储体检 fail-closed、`_rebuild_positions_from_broker`（prewarm+重连）、`_capture_day_open_equity` 持久化 OPEN 基线同日复用、卖单 revert 钩子、REDUCE 整手、卖出巡检 per-unit 异常隔离+helper、mark_selling 竞态防御（#19#31#37#90 + 复审 P1）。
- `data_writer/callbacks.py`：`on_stock_trade` 改"内存权威先于 qmt 镜像"且镜像异常隔离、卖单终态失败 `_maybe_revert_sell_unit`（#62#31）。
- `app/run.py`/`app/scheduler.py`：存储故障钩子+健康探测器接线、盘中周期存储体检。

**review（独立审计 agent，资金安全口径）**：确认 P0 主修真实闭环（flush_confirm 不漏判、QMT 权威 can_use 不被覆盖、卖单 revert 四类全覆盖、committed 口径自洽）；查出并已修：
- 复审 P1-1：`_place_sell` 发单后 `mark_selling` 与回调线程秒成存在竞态会抛错中断整轮 → 已加状态防御 + `run_sell_pass` per-unit try/except（提取 `_evaluate_and_sell_unit`）。
- 复审 P1-2：`PositionManager._units` 无锁，回调线程 rebuild 批量改字典与调度线程迭代并发会崩 → 已加 RLock 保护结构性写入/迭代快照（#61 的崩溃面在本轮提前堵；字段级细粒度竞争仍留阶段E）。
- 复审 P2：`flush_confirm` 注释错位、基线落盘用 flush 非 confirm 的措辞、rebuild 去重边界 → 已修注释/降级措辞/加口径说明。

**测试**：新增 `tests/test_round1_stage_a.py` 共 22 例（含写队列 fail-closed、持仓重建/复位、TTL 撤单、卖单 revert、回调顺序、基线复用、整手减仓、per-unit 隔离、并发 rebuild 不崩）。全量 **471 passed**（基线 449 + 22）。

**本轮一并处理（从后续阶段提前）**：#90（卖出巡检异常隔离，原属阶段E）、#61 的崩溃面（`_units` 加锁，原属阶段E；字段级竞争仍留 E）。

**提交**：执行侧 `astock-quant-ai` main 分支（见 commit）。

> 说明：原计划的 21-agent 并行验证 workflow 运行中疑似停滞（仅 9/21 返回、未送完成通知），故 Round 0 的"问题是否真实"改由**逐文件直接读码核对**完成（每条 finding 在实现前都打开真实代码确认），结论见各轮 §x.1 分析表。
