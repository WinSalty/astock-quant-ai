# 待办与上线验证清单

> 本文记录**还需要处理的事**，以及**只有真实上生产/目标机才能验证（实测）的事**。分工：
> - [`README.md`](README.md)：项目现状口径（交易逻辑/架构/部署设计/资金口径）。
> - [`评审与修复状态概要.md`](评审与修复状态概要.md)：评审做了什么/修了什么/状态（指向 08 报告 / 09 修复台账详情）。
> - **生产部署/重启/token/计划任务/连通性自检**：本机运维手册 `/Users/salty/codeProject/ai/doc/Qmt生产服务器.txt`（含执行侧 Windows bring-up；非本仓库、含密码勿外泄）。
> - 已完成、不再维护的历史设计/计划/旧评审 → `已归档-完成不再维护/`（不再看）。
> - 本文是「活」的待办，处理完一项就划掉一项；全部清空后本项目即达到可放量上线状态。

更新口径：相对日期一律转绝对日期。最近更新 2026-06-16（第三轮评审修复收尾：门槛 A–E + 模块迭代 13 批全清零，详见 [`评审与修复状态概要`](评审与修复状态概要.md) 第三轮段与归档台账 `已归档-完成不再维护/14-第三轮评审修复计划与执行台账.md`）。**2026-06 go-live 审计补：新增 §0「上线前必做代码项」（盘中卖出链接线，硬阻塞·非实测，不计入「13 批全清零」口径）。** **2026-06 国金 QMT 对接专项核对补：§0 增卖出链字段映射规格 + `prior_provider` 同根必补；§A 增 A8–A10 + 「占位值已核对迅投源码为标准正确值」澄清 + 国金客户经理须确认事项；§D 增国金对接核对 4 项（255 状态映射 / cancel try/except / `QMT_ACCOUNT_TYPE` 惰性 / MySQL conn_factory）。分阶段开发计划见 [`16`](16-卖出链接线与上线前代码缺口开发计划.md)。**

---

## 0. 上线前必做代码项（硬阻塞，**非实测**，必须离线补齐）

> 本节是「配好环境也跑不通、必须改代码」的硬阻塞，独立于 §A 真机实测项；清空本节是放量上线的前提之一。

- [ ] **[代码·离线·硬阻塞] 接线盘中卖出链**：当前 `qmt_strategy/app/run.py:334 sell_books_provider=None`（无 env 开关）、`scheduler.py:142` provider 为 None 即 `return` → `run_sell_pass` 生产**永不执行**；全仓非测试代码**零 `OrderBook()` 构造点**。⇒ 买入后结构上**无法主动卖出**（止损 / 炸板 / 破位 / 连板了结 / T+1 常规卖出全不触发，裸奔扛单）。
  - **要做**：实现 `sell_books_provider(today) -> {ts_code: OrderBook}`（由 xtdata 实时盘口派生封板/炸板/破位/放量/量价背离等盘中信号，**样板见 `auction/auction_factors.compute_auction_factors`**）→ 在 `run.py` 注入 `DailyScheduler(sell_books_provider=…)`（连带 `prior_provider` 续板先验，同根未接）→ 补 fake 盘口源**离线单测**覆盖 `run_sell_pass` 触发 / 止损 / 炸板 / 破位 / T+1 卖出各分支。
  - **字段映射规格（2026-06 国金对接核对补，分两步落地）**：`OrderBook`（`contracts/models.py:314`）共 13 字段，`sell_decider` 实际消费的可分两类——
    - **可立即离线落地（复用买入侧 `auction_factors` 单帧样板）**：`last_price`←`tick['lastPrice']`、`open_pct`←`open_pct(lastPrice,lastClose)`、`seal_to_float_ratio`/`seal_amount`←`virtual_seal(tick,limit_up_price,float_mktcap)`（封单额=`_best_level` 取五档 best 的 `bidVol×bidPrice`）。先接这 4 个即可让**弱开判据 / 封流比 / 单票浮亏止损（`_unit_stop_loss_breached`）**生效。
    - **须真机 tick 实测后才能保真（跨帧状态量，无单帧样板）**：`is_sealed` / `broke_board`(炸板) / `below_support`(破位) / `volume_surge` / `near_close_weak` / `open_times` / `price_volume_diverge`——需先在国金真机用 `vars(tick)` 固化「涨停价 / 分时均价 / 成交量量纲」键名，再实现一个保留 per-code 帧历史的 builder（与 `AuctionPoller.prev_ticks` 同思路）。**固化前这 7 项保守置默认（decider 走 HOLD/弱开兜底、不会误清仓，但破位/炸板止损此时缺失）**，须随 §A tick 实测一并完成。
  - **`prior_provider` 续板先验同根必补（同批做）**：`run.py:236` 仅 TODO、`build_real_engine` 未注入 → `EngineDeps.prior_provider` 落默认 lambda 恒 `None` → 跨买入日持仓恒 `mode=TECH_EXIT` 纯技术退出，`sell_decider._reduce_or_clear` 因 `prior_strong=False` **恒选 CLEAR 不选 REDUCE**，信号侧续板增强（`continuation_prob`/角色/保底仓）全部不生效。要做：实现 `prior_provider(ts_code,today)->Optional[SignalPrior]` 读本机当日 `limit_up_selected_stock`/watchlist 表（取不到返回 None，单票异常已由 `main.py:939-943` try/except 隔离）。**非资金安全阻塞**（无先验=纯技术退出，安全方向正确），但卖出链接通后才有意义，应同批接线；短期不接须在文档明示「当前为纯技术退出、续板续持/减仓保底仓未启用」。
  - **口径**：这是**可离线写完 + fake 单测自证**的代码缺口，**不是**真机绑定（与 §A 实测项区分）；唯一真机依赖是「原始 tick 字段键名」，与买入侧竞价四因子同批、已在 §A 登记。**接线前严禁接实盘**（买入即裸奔无止损）。历轮评审（08、codex P1-2.9、第三轮 EXEC-risk-04 / EXEC-position-08）均点过此缺口但被当 TODO 延后，**不计入「13 批全清零」口径**。详细分阶段开发计划见 [`16-卖出链接线与上线前代码缺口开发计划.md`](16-卖出链接线与上线前代码缺口开发计划.md)。

---

## A. 目标机实测项（[实测]，没有真机/miniQMT 跑不了）

> 都需要在 **Windows VPS + 国金 miniQMT 登录后** 跑一次性核对，把占位值固化回代码。
>
> **口径**：本节仅列**必须真机/miniQMT 才能核对固化**的项（盘口结构 / `xtconstant` 常量 / 回调签名等）；**盘中卖出链的「接线代码」(provider + 注入) 属可离线完成的上线前必做代码项，见 §0，不在本实测桶内**——勿把两者混为一谈。
>
> **重要补充（2026-06 国金对接核对：占位值已联网核对迅投 xtquant 源码，实测=最终确认而非纠错）**：经核对迅投 xtquant 官方源码/文档（`xtconstant.py`、dict.thinktrader.net），代码中标「占位/待实测」的关键值**实为标准正确值**：`STOCK_BUY=23`/`STOCK_SELL=24`/`FIX_PRICE=11`、委托状态 `48–57` 映射（含 54 已撤/56 已成/57 废单三终态无偏移）、`order_stock` 8 参顺序、7 个回调方法名、持仓证券代码字段用 `stock_code`（**未踩 `m_strInstrumentID` 取空坑**）、`order_id<0` 判失败（正确覆盖官方失败码 -1，优于常见误写 `is None`）、`session_id` 收口正 int32（规避 C 层溢出）。国金 QMT = 迅投 QMT（ThinkTrader/iQuant）定制版，xtquant 完全通用。故 §A 实测的性质是「上真机用 `vars()`/`dir()` 核对一遍、把『占位』字样去掉钉死」，**不是「现在写错了等着改」**——代码大概率可直接跑通；唯需逐回调/首笔实盘验证回调确被触发（方法名错无 getattr 兜底）。

| # | 实测什么 | 怎么测 | 固化到哪 |
|---|---|---|---|
| A1 | `xtdata.get_full_tick` 返回结构 | `from xtquant import xtdata; print(xtdata.get_full_tick(['000001.SZ','600000.SH']))` + `vars()` | `auction/auction_factors.py` 取值键名 |
| A2 | 五档是 list 还是分列 `bidPrice1..5` | 看 A1 输出的 bidVol/bidPrice/askVol/askPrice 结构 | `auction_factors._best_level`（已按 list 取 best 档，键名/结构待确认） |
| A3 | `volume` 单位（手 / 股） | A1 输出对照真实成交量 | 与竞价量能比分母 `first_board_vol`（手）对齐；不一致则换算 |
| A4 | **竞价段(9:20 前)能否拿到 `bidVol`(虚拟封单)** | 9:15–9:25 实跑轮询看 bidVol 是否有值 | 决定「竞价择时能不能开」；拿不到则一键降级只看 `last_price`+开盘后确认 |
| A5 | `xtconstant` 数值：STOCK_BUY / STOCK_SELL / FIX_PRICE | `from xtquant import xtconstant; vars(xtconstant)` | `order/order_executor.py` 的 `XT_ORDER_TYPE_BUY=23 / SELL=24 / FIX=11` 占位 |
| A6 | 回报对象字段名 `XtTrade/XtOrder/XtAsset/XtPosition/XtOrderError/XtCancelError` | 实跑回调打印 | `data_writer/normalize.py` 的 getattr 键 + `_STATUS_NUM_MAP` 占位 |
| A7 | `order_stock` 真实参数顺序、`StockAccount` 构造签名、回调方法名/签名 | 对照 xtquant 文档 + 实跑 | `adapters/xt_real.py` 的 `TODO(实测)` |
| A8 | `order_stock` 同步失败返回值（确认为 -1，非 0/None）| 故意废单（停牌/超价）观测返回值 | `order_executor.py:491/657` 失败判定（现 `is None or <0` 已覆盖 -1，可选收紧为 `<=0`）|
| A9 | `cancel_order_stock` 对**已成交/不存在 order_id** 的行为（返回 -1 还是**抛异常**）| 实盘对一笔已成单发撤单观测 | 决定是否给 `order_executor.py:924/973` 两处 cancel 加 try/except——若抛异常会打断 `sweep_expired` 整轮巡检致同批其它到期单漏撤（见 §D 国金对接核对新增）|
| A10 | `StockAccount` 是否需 `account_type`（纯现货 vs 两融）+ `order_remark` 长度上限（官方建议 ≤20，当前格式 `LUP\|YYYY-MM-DD\|600000.SH`≈24 字符）| `vars(StockAccount)` 签名核对 + 实盘观测 remark 是否被券商截断 | `xt_real.make_stock_account`（现仅单参，纯现货 OK；两融需 `'CREDIT'`，并接 `QMT_ACCOUNT_TYPE`）+ `order_executor._REMARK_MAX_LEN`（即便券商截断也保住 `LUP\|<日期>\|` 头、`signal_trade_date` 回填不丢）|

> 详见 `adapters/xt_real.py` 顶部 `TODO(实测)` 清单。实测前：`KILL_SWITCH` 开、竞价择时(`QMT_AUCTION_TIMING_ENABLED`)保持关。
> **国金特定·须向客户经理书面确认（非代码项，影响能否上线）**：① 个人账户是否开放**程序化下单**（部分券商 iQuant/miniQMT 个人只给行情、下单仅对机构）；② 是否需单独签**程序化交易权限协议** + **2024 起监管要求的程序化交易报备**是否完成；③ L2/十档行情是否收费、是否在 miniQMT 可用（打板类需要）；④ 报单/撤单流控阈值；⑤ 资金门槛（常见 50 万，可协商）；⑥ `userdata_mini` 安装路径（建议非 C 盘、无中文，形如 `D:\国金证券QMT交易端\userdata_mini`）；⑦ 是否需升级「投研版」才能用某些 xtquant 功能。

---

## B. 上线部署步骤（必须按序做，否则报错/拒启动）

1. **信号侧跑数据库迁移**（F3 新增两列）：
   ```bash
   cd .../stock-ah-premium-ai/backend && . .venv/bin/activate && alembic upgrade head
   ```
   不跑 → 选股落表因缺 `float_mktcap/first_board_vol` 列报错。
2. **执行侧提供交易日清单文件**（E1 fail-closed 需要）：
   ```bash
   # 信号侧导出（脚本 backend/scripts/export_trade_calendar.py）
   python scripts/export_trade_calendar.py --output /opt/qmt/trade_days.txt --min-future-days 90
   # scp 到 Windows 交易机后配：
   QMT_TRADE_CALENDAR_FILE=C:\qmt\trade_days.txt
   ```
   未配且未显式放行 → 进程启动期 RuntimeError 拒绝启动。每年节假日安排公布后重新导出 scp。
3. **资金/风控关键开关**（建议显式配，口径详见 README §7 资金分配口径）：`QMT_MAX_POSITIONS_PER_DAY`(单日建仓只数上限,默认5)、`QMT_TARGET_POSITION_RATIO`、`QMT_PER_ORDER_MAX_AMOUNT`、`QMT_MAX_POSITION_PER_STOCK`、`QMT_MAX_TOTAL_EXPOSURE`、`QMT_ACCOUNT_DRAWDOWN_LIMIT`/`QMT_STOCK_FLOAT_LOSS_LIMIT` 等。
4. **上线硬门槛**（不可颠倒）：A 段实测全过 + 回测 `go/no-go` 代码化裁决为 GO（非人工看数）→ 小仓灰度 → 逐步放量；竞价择时先关、实测过再灰度开。

---

## C. 需要你（业务方）拍板的口径

- **资金分配权重形态**：目前「强度线性加权」（强度 90:30 → 预算 3:1）。若要更重仓龙头，可改凸权重（强度²）或只分 Top-N；是否把 `role=龙头/中军` 并进权重（角色加成）。
- **`first_board_vol` 口径**：已按「信号日全天量」（首板票即首板量）落地；若要连板取「真首板日量」需回算，确认是否升级。
- **go/no-go 硬阈值**：当前为保守占位（最小样本/胜率/超额/IC/p10），待回测/实盘数据校准后随版本固化。
- **「谨慎参与」是否允许开仓**：已按「禁开仓」实现（仅「参与」开仓），如需放开请说明。

---

## D. 代码侧优化待办（非阻塞，多需回测校准）

> 详细逐项见评审报告 [`08`](已归档-完成不再维护/08-信号侧与执行侧量化交易代码评审报告.md) 与修复台账 [`09`](已归档-完成不再维护/09-两侧代码修复计划与执行清单.md) 剩余项。

- 回测：显著性检验(CI/bootstrap, D2)、默认含费净收益口径(D3)、LLM 续板/溢价概率校准(D4)。
- 选股：龙头打分用「全涨停池」做分位基准（P1#5）；role 在仓位/择时分级消费。
- 风控：黑名单（低优）；卖出对手价/跌停保成交价 + 滑点下保护（依赖 A1–A4 实测的盘口买一价）；`order_executor` 层通用限价偏离闸 `price_deviation_guard_pct`。
- 供数/可观测：复权口径在指标窗口的除权处理；情绪/宽度统计含/不含 ST 口径；回流映射命中率自检告警。
- **第三轮评审遗留的经验占位常量（待回测/数据到位后标定固化）**：
  - 竞价量能比 `_VOL_RATIO_MIN`（`entry/strategies/chase_auction_strong.py`）：当前分母用首板日**全天量**、量纲/时段不对等致比值结构性偏低，已降级为 `weak_vol` 留痕**不再单独硬弃**（EXEC-auction-05）。终态需信号侧补「首板日**同时段(9:15–9:25)竞价量**」字段（PlanRow 增 `first_board_auction_vol`，由分钟数据回填）使量纲对等后回测标定，再把本判据重启用为硬门槛。
  - 缺强度/beyond-N 份额系数 `_MISSING_STRENGTH_FACTOR`（`app/main.py`，当前 0.1）：缺强度票落入 top-N 时的保守份额基数系数，经验占位待回测标定（EXEC-entry-05）。
  - 台账保留窗口 `DEFAULT_LEDGER_KEEP_DAYS`（`storage/sqlite_ledger.py`，当前 14）：`load_from_db` 窗口装载 + `purge_before` 盘后清理的保留天数，经验取「T+1 + A股最长 9 天假」留余量，按实际最长在途周期复核（EXEC-storage-05）。
  - 公开分享默认有效期 `limit_up_push_share_default_expire_hours`（信号侧 config，当前 168h=7 天）：永久链接默认禁用后的默认窗口，按运营需要调（SIG-DLV-05）。

- **go-live 审计新增（2026-06，非阻塞，可离线择机做）**：
  - **（建议随卖出链同批补，优先级偏高）** 启动期 fail-closed 校验 `QMT_ACCOUNT_ID` / `QMT_MINI_PATH`（`run.py:219` 缺配静默回落 `account_id="UNKNOWN"`、`run.py:255` `mini_path` 缺配传空串，均无断言；参照交易日历 fail-closed 模式）。**风险**：误启动会用假账户号 `UNKNOWN` 贯穿台账/持仓单元键/`qmt_*` 四表 `account_id` 列/回流信封/对账，真实资金场景口径污染、串账户、与正确账户台账永久错位。生产 `run_qmt.bat` 虽 env 显式配，但代码层无强制守卫。
  - `LocalStorage` 初始化对 `QMT_LOCAL_DB_PATH` 父目录 `makedirs(parents=True, exist_ok=True)`（全树零 makedirs，配成父目录不存在的绝对路径则 sqlite 启动崩；默认相对路径安全）。
  - 盘后 SYNC 检查 `sync_to_remote` 返回的 ok/errors，部分回流失败升级 `logger.error`/告警而非无条件 `info`（`scheduler.py:239-241`；当日 SYNC 一次性、失败行保 `synced=0` 不自动重跑，需补「历史 synced=0 可手动重同步」运维入口）。
  - 低优清理：`assert_safe_to_trade` 空 no-op（`settings.py:316`，误导性 API 且**全仓无调用方**）、`PositionState.FROZEN` 态无赋值点、`QMT_AUCTION_WINDOW`/`QMT_INTRADAY_SNAPSHOT_MINUTES` 惰性配置（被解析不消费）、`try_next_best` 因 `next_best` 恒空而休眠。

- **国金对接核对新增（2026-06，前轮文档未登记，非阻塞·可离线）**：
  - `_STATUS_NUM_MAP` 缺 `255`(ORDER_UNKNOWN) 映射（`normalize.py:105`）：未知委托态兜底落 `REPORTED`(在途)；若某废单/异常单实际回 255，会被长期当在途、卖单 `SELLING` 不复位漏重挂。建议显式补 `255 → REJECTED`（或保留兜底但加注释说明故意留在途）。
  - `cancel_order_stock` 两处未包 try/except（`order_executor.py:924`/`973`）：若国金对已成交/不存在 order_id 的撤单**抛异常**（见 §A9 实测），异常会冒泡打断 `sweep_expired` 整轮巡检 → 同批其它到期单当轮漏撤、TTL 兜底中断。建议加 try/except 留痕（与 §A9 实测联动）。
  - `QMT_ACCOUNT_TYPE` 全链路惰性（`settings.py:60`）：解析了但 `make_stock_account` 不透传、`qmt_*` 记录的 `account_type` 来自回报对象而非本配置——配了不起作用（与 `QMT_AUCTION_WINDOW` 等惰性配置同类）。结合 §A10 确认 `StockAccount` 是否需 `account_type`：需要则透传（注意官方第二参是字符串 `'STOCK'`/`'CREDIT'` 而非 settings 当前的 int），否则删除以免误导。
  - MySQL 回落同步通道 `conn_factory` 为 `NotImplementedError`（`run.py:122`）：配 `QMT_MYSQL_DSN`（不配 `signal_base_url`）即盘后 `RemoteSyncJob` 每行 upsert 抛错、回流静默全断且无 error 告警。**默认走 HTTP 回流不触发**；建议在 settings 注释/文档标注该回落不可用，或 `_build_remote_repo` 配 DSN 时启动期直接报错而非延迟到盘后静默失败。（注：`http_ingest`/`repository` 的只读方法 `NotImplementedError` 已由 `reconcile.py:559` catch 降级到本地 SQLite，属设计内非缺口。）
- **承接 doc/10 未落地项（有外部硬前置，doc/10 暂未归档原因）**：
  - §3.1 moneyflow 资金主线（全市场拉取 → 归一化并入 compact → 改 `_money_subscore` 及依赖链 §4.3/§5.3②）；前置=积分档 `trade_date`-only 全市场放行脚本验证 + 涨停/入选池覆盖率抽样 + 单位换算固化 + bump `scoring_version` 与回测同批迁移。
  - §6.4 子分随回测落库 + `WEIGHTS_V2` 单调约束回归（依赖实盘 `qmt_*` 回流标签——其闭环受 §0 卖出链未接线结构性阻塞）。

## E. QMT 实盘复盘看板深化（首版已上线，深化依赖新表/新数据）

> 首版「实盘复盘」菜单（信号侧前端，仅 admin）已落地：当日复盘 / 历史净值 / 持仓 / 交易质量 4 Tab + `/api/review/*` 只读 API。方案与已完成/延后明细见信号侧 `resources/doc/qmt-review-dashboard-dev-plan.md` §10。以下为延后项：

- [ ] **出入金台账 `qmt_cash_flow`**：建表 + admin 录入 UI；落地后历史净值改用精确 TWR（当前为 `total_asset` 简单归一，未剔出入金）。
- [ ] **FIFO 交易级盈亏**：委托-成交配对 + 持有时长计算；落地后「已实现盈亏」由近似（当日盈亏−浮动）改精确，并支撑交易质量 Tab 的单票胜率/盈亏比/持有天数。
- [ ] **滑点与买不进归因**：成交价 vs 委托价/信号价基准快照；买不进细分（封单不足/撤单/涨停打开）。当前交易质量 Tab 仅有成功率/买不进计数。
- [ ] **闭环归因物化**：计划→挂单→成交漏斗、逆向选择、先验校准、空仓闸门反事实（信号侧已有归因设计文档，需物化表+计算）。当前仅在成交明细以回挂信号 Tag 呈现关联。
- [ ] **账户↔用户绑定 `qmt_account` + 多账户隔离鉴权**：当前 admin 可见全部账户；落地后按账户做 per-user 数据隔离。
- [ ] **实测**：执行侧真实回流一段交易日数据后，端到端核对看板各口径（当日盈亏剔出入金是否正确、净值/回撤/夏普、信号 join 命中率）。

## F. 决策链路看板（信号选股 + 执行决策明细）—— 阶段 1-3 已上线，阶段 4 待真机验证

> 已落地：信号侧「信号选股」「决策流水/闭环」Tab + `qmt_decision_log` 表 + ingest 接收端（信号侧 feature 分支）；执行侧 `DecisionEmitter` 非阻塞采集 + 各决策点埋点 + best-effort 回流（执行侧 main 分支，`decision_log_enabled` **默认开**）。设计见信号侧 `resources/doc/qmt-decision-trail-design.md`。以下为**仅能真机验证 / 部署口径**待办：

- [ ] **[实测] 默认开的采集对实盘下单零影响**：真机 miniQMT 跑一遍竞价/盘中真实下单，确认下单链路时延无可观测增加、`qmt_trade/qmt_order` 不受影响；队列压满时 `dropped` 增长但消费线程不崩、交易继续；kill 消费线程/断回流时交易与四表回流完全正常（故障注入）。
- [x] **[部署·信号侧] watchlist 导出接口 token 已配 + HTTP 接口已打开**（2026-06-14）：信号侧生产 `111.231.21.183` 已配 `WATCHLIST_EXPORT_INTERNAL_TOKEN_FILE`（token 文件 `secrets/qmt-internal-token.txt`），`/internal/watchlist` 由 503→带 token 200、无/错 token 401。
- [ ] **[部署·信号侧·上线陷阱] 单独配置 ingest 写 token**（go-live 审计新增·原 [x] 项口径已过时）：第三轮 SIG-QMT-06（token 按接口隔离）起，ingest 鉴权 `resolve_qmt_ingest_internal_token`（`backend/app/core/config.py:468`）**已不再回落** watchlist 导出 token——信号侧必须**单独**配 `QMT_INGEST_INTERNAL_TOKEN(_FILE)`；缺配则 `POST /internal/qmt/ingest` 恒 503、执行侧盘后回流静默断流（无人告警）。**当前生产仅配了 watchlist 导出 token，ingest 写 token 尚未单独配**。执行侧侧用**与之相同的值**配 `QMT_SIGNAL_INGEST_TOKEN(_FILE)`（未配则回落用 `QMT_SIGNAL_INTERNAL_TOKEN`），须保证「执行侧实际发送的 ingest token == 信号侧 `QMT_INGEST_INTERNAL_TOKEN`」。（信号侧 `routes_qmt_ingest.py:8` 注释「默认复用 watchlist 导出 token」为过时注释，已随本轮一并清理。）
- [ ] **[部署·执行侧] 配 `QMT_SIGNAL_BASE_URL` + `QMT_SIGNAL_INTERNAL_TOKEN(_FILE)` = 信号侧同一 token**（token 值在信号侧 `secrets/qmt-internal-token.txt`，勿明文外泄）。
- [ ] **[实测] 决策→看板闭环端到端**：真机产生的决策回流后，看板「决策流水/闭环」能完整呈现「信号达标→下单/未买→卖出」时间线，且与执行侧日志、`qmt_trade` 成交一致；东八区展示与 UTC 存储无 ±8h 偏差；reason_code/miss_reason 映射正确。
- [ ] **一键回滚演练**：`QMT_DECISION_LOG_ENABLED=false` 重启 → 采集退化 no-op（不起线程），确认交易与四表回流不变。
- [ ] **信号侧合并**：决策链路相关接口/表/前端在信号侧 `feature/limit-up-watchlist-signal` 分支，需并入主干并随后端迁移 `0055` 上生产。

## G. 生产部署现状与执行侧 bring-up（2026-06-14 勘察）

### 信号侧 `111.231.21.183`（Linux）—— ✅ 已部署最新代码
- rsync 同步最新代码（feature/limit-up-watchlist-signal 头 `7d23c67`）；Alembic `0052→0055`（**全为新增表/可空列，未改任何业务数据**）；前端 dist 重建同步；后端/前端重启，三服务 active、health ok、tushare/llm/qwen/pushplus 全 true。
- 内网 token 已配（见 F 段）；`/api/review/*` 路由 live；生产 admin 已加 `qmt_review` 菜单权限。
- 复盘看板读真实数据正常（最新信号日 2026-06-12 / limit-up-v1 / 入选 13）；qmt 回流账户为空（执行侧尚未回流，符合预期）。

### 执行侧 `115.159.81.231`（Windows，C:\qmt\astock-quant-ai）—— ✅ 已 bring-up（仅差 miniQMT）
勘察结论（部署前为「已部署但休眠未配置」）：
- 代码：git 仓库、分支 `main`、停在 **`5b324e3`（2026-06-13）**，**落后当前 main 37 个提交**（缺全部 P0/P1 执行侧修复 + DecisionEmitter + 单日建仓只数上限等）；工作树干净（仅未跟踪 `egg-info`）。
- 环境：Python 3.11.9 + venv（`qmt_strategy\.venv`）就绪；git 已装。
- **未配置**：无任何 `QMT_*` 环境变量、无 `.env`/config 文件 → account_id/signal_base_url/token 全默认 None；**无计划任务、无运行中的 python 进程、无启动脚本** → 该机当前**未实盘运行、从未与信号侧连通**（信号侧日志也无该机远端调用）。
- 网络连通性 ✅：执行机 → 信号 `:8000/api/health` 200、`/internal/watchlist` 401（可达 + 接口 live，仅缺 token）。

执行侧 bring-up 进度（2026-06-14 已做「不依赖 miniQMT 的全部内容」）：
- [x] `git pull origin main`：fast-forward `5b324e3→00f4276`（最新）。
- [x] `qmt_strategy\.venv\Scripts\python.exe -m pip install -e '.[dev]'`：依赖(tzdata)+pytest 装好。
- [x] `pytest -q`：全绿（用 fake，不需 miniQMT）。
- [x] 配置文件就位：`C:\qmt\config\trade_days.txt`（信号侧导出，727 日至 2026-12-31）、`C:\qmt\config\signal-token.txt`（信号侧同一 token，scp）。
- [x] 启动脚本 `C:\qmt\run_qmt.bat`：含全部 `QMT_*`（`QMT_SIGNAL_BASE_URL`/`QMT_SIGNAL_INTERNAL_TOKEN_FILE`/`QMT_TRADE_CALENDAR_FILE`/`QMT_LOCAL_DB_PATH`/`QMT_KILL_SWITCH=true`安全默认/`QMT_AUCTION_TIMING_ENABLED=false`/`QMT_MAX_POSITIONS_PER_DAY=5`/`QMT_DECISION_LOG_ENABLED=true`）+ 待填 `QMT_ACCOUNT_ID`、`QMT_MINI_PATH`。
- [x] 验证（`bringup_check.ps1`，不下单）：config 全解析（token_resolved True、calendar 727 日）、带 token 拉信号侧名单 **200 / count 13**。
- [ ] **[需 miniQMT]** 装 miniQMT 客户端 + run_qmt.bat 填 `QMT_ACCOUNT_ID`/`QMT_MINI_PATH` + venv 对接 xtquant。
- [x] 计划任务模板已就位（`C:\qmt\`：`register_qmt_task.bat` 注册脚本 + `stop_qmt.bat`）：单进程常驻、内部 DailyScheduler 按东八区驱动各相位，故只需「工作日 08:45 start（先清残留防双开）/ 15:45 stop」两个任务、用 `/IT` 在交互会话跑。**待 miniQMT+账号就绪后【管理员】跑一次 `register_qmt_task.bat` 注册即可**（前置：miniQMT 自启自动登录、Administrator 保持交互登录态）。
- [ ] **[需 miniQMT]** 真机首日 `QMT_KILL_SWITCH=true` 只观察：连通拉名单、决策采集对下单零影响、回流落 `qmt_*`/`qmt_decision_log`、看板闭环可见 → 无误后改 false 放量。

---

*处理完一项即划掉。整份清空（含 §0 代码硬阻塞 + A 段实测）后，本项目方达到「可按 README §5 上线硬次序 + 本清单 §B4 硬门槛放量上线」的状态。*
